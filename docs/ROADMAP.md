# Roadmap

Ordered list of work items, top to bottom. Earlier items are higher priority than later items. This roadmap is the authoritative source of "what's next" for legalOS; CHATBOT_HANDOFF.md points here rather than maintaining its own deferred-work list.

The order reflects current operator priority and is expected to shift as items ship, new items surface, and product direction evolves. Reordering this list is a normal part of regular work, it doesn't require a decision-log entry.

Shipped work is marked done in place (with its decision-log reference) rather than deleted, so this file doubles as a record of what landed and when; the forward-looking items are what remains. New items get inserted at the appropriate position. The Backlog section at the bottom holds items not yet prioritized; promote to the main list when an item is worth real work.

---

## 1. Admin polish arc (admin information architecture, designed fresh) — CLOSED (A7, 2026-06-01)

**Arc status: the admin arc is closed out (A7).** The GOVERN side is COMPLETE: Policy & access (A2/A2b), People (A3a/A3b/A3c), and the Audit log (A6). The MEASURE side is INTENTIONALLY DEFERRED (not unfinished by accident): Insights A4a shipped as a functional placeholder pending a delight pass; the Insights cost/ROI lens A4b is deferred pending the business-model decision (section 1a); Evals A5 is deferred as an open design question. The historical framing below is retained as the record of how the arc was scoped.

The connector hub arc shipped (Settings as a peer mode, the capability-grouped Connections page, the connections/grants/policy data model with RLS, Google Drive OAuth end to end, policy enforcement, live Drive reads in agents, and the Drive file picker with search and folder browsing; D-062 through D-072). The deferred Admin work was the next major arc, and it is deliberately architecture-first.

The admin information architecture is undecided and gets designed fresh: all admin functions, roles, and surfaces thought through up front before more admin-dependent features accrete on top. The existing admin pages (Productivity Calculator, Adoption Metrics, User Access) are placeholder-quality and get brought up to the polish standard in this arc.

The admin connection-policy editing UI lives here (deferred from the connector arc per D-066): policy enforcement shipped, but the super-admin UI to edit the policy (allowed categories, allowed providers, the capability ceiling) is built in this arc, where the admin IA is designed fresh. Until then the `connection_policy` row is edited directly in the database.

Three sub-areas surface within this arc, each substantial enough to be its own multi-stage effort:

- Admin analytics: comprehensive admin dashboards, metrics, reporting (relates to roadmap item 17; analytics promotion to Supabase is a prerequisite).
- Evaluations / testing: some form of agent evaluation, A-B testing, or quality measurement surface for admins to assess agent performance.
- Scalable department configuration: admin tooling to manage departments at scale (faster than the per-row admin UI, possibly bulk operations, possibly a different data model for department configuration).

Admin adopts the settings primitives (the portability principle): the refined-list landing, the considered register, the left-justified layout, flat-at-rest rows with hover-highlight, grounding state-dots, and the network-backed-UI loading standard (skeleton-on-open plus cross-fade). Likely absorbs the admin-config-surface and matter-workspace-management backlog items when this work starts.

The IA is now decided (D-074): admin's two jobs, govern the use and measure the value, with four areas, People and Policy & access under govern, Insights and Evals under measure.

**A1 (shell) shipped (2026-05-31):** the admin rail (lead-line "Admin" + the govern/measure captioned groups), the refined landing introducing each area, and four coming-soon area stubs, with admin reconciled to the 896px settings-family width.

**A2 (Policy & access editor) shipped (2026-05-31):** the super-admin UI to edit the org's connection policy (the capability ceiling as a read-only / read-and-write trust statement, and allowed connection categories with providers derived server-side), gated to super admins via a net-new `isCurrentUserSuperAdmin()`; other admins see it read only. Closes D-066 (the deferred connection-policy editing UI); the policy no longer needs direct-DB edits. See D-076.

**A2b (org default model) shipped (2026-06-01):** the third Policy & access governance control, the org-level default model new agents start with, stored super-admin-only on the organizations row (read-only for other admins). The product's scattered model lists were consolidated into one canonical models source feeding validation and every picker, and Claude Opus 4.8 was added as the new flagship and default (joining the composer quick-pick alongside Sonnet 4.6 and Haiku 4.5; older Opus versions stay in the full form picker). Policy & access is now complete: capability ceiling, allowed categories, and default model. See D-078. (Update 2026-06-09, D-147: Claude Fable 5 added to the canonical models source and made the flagship and default, with its id and per-token pricing, including cache rates, verified against Anthropic's official docs so the cost analytics stay accurate; Opus 4.8 moved to the full picker, then the selectable set was trimmed to one per tier — Fable 5 / Sonnet 4.6 / Haiku 4.5 — with the Opus generations retained unselectable so existing agents and historical cost records keep working (D-148). Model discovery/approval/retirement automation remain the future Connections-phase arcs.)

**A3a (People roster + roles) shipped (2026-06-01):** the People area replaces the old users page's roster, department-access, and new-user default-departments with an A2/A2b-register surface, and adds an in-product org-role editor governed by a least-privilege escalation rule (only a super admin grants super admin; an org admin manages user↔org-admin only; the last super admin cannot be demoted; self-demotion confirms). The rule is enforced in three layers (honest UI, server action, and a tightened RLS/trigger that closes the prior privilege-escalation hole). Every role change is recorded to a new `role_change_audit` table, which feeds the future A6 audit-log UI. See D-079. The old users page stays reachable but unlinked, retiring once People fully supersedes it (after A3c).

**A3b (soft-deactivation) shipped (2026-06-01):** `users.is_active` is now load-bearing. Admins deactivate/reactivate a person from the roster; deactivation is a reversible soft block (flips one flag, destroys nothing) enforced at the request layer (proxy per-request cutoff, auth-callback new-session reject, workspace-layout page guard), deliberately not via RLS. Gating mirrors A3a (org_admin can't deactivate a super_admin; the last active super_admin can't be deactivated; self-deactivation confirms), and the A3a role trigger's last-super-admin count was tightened to require active status. Every status change records to a new `user_status_audit` table. See D-080.

**A3c (invitation) shipped (2026-06-01) — PEOPLE COMPLETE.** Admins invite by email with a chosen role + departments; the invite rides Supabase's auth email via the service-role `inviteUserByEmail` (no new email infrastructure), and acceptance is seamless (provisioning consumes the pending invite on first sign-in, applying the chosen role/departments). The env-var allowlist is replaced by a DB-backed gate (admit invited OR existing user, env list kept only as a safety hatch, never fail-closed-on-unset, so the owner is never locked out). Pending invites are resent/revoked on People, same escalation gating. **The old admin Users page is retired** (308 redirect to People; its duplicated components removed). People now covers roster, roles, deactivation, defaults, and invitation. See D-081.

**A4a (Insights usage/adoption lens) shipped (2026-06-01) — FUNCTIONAL PLACEHOLDER pending a delight pass:** the first MEASURE area's first lens, a measured view of native-agent activity built on `usage_events` by generalizing the home impact-math query pattern org-wide. Total agent runs and trend over a chosen window (week/month/YTD), broken down by agent, department (via the agent, the clean rollup), model, and person, plus an adoption-gap signal (agents with zero runs). A clearly-labeled demo sample-data toggle previews the experience before real usage accrues. Org-admin gated; no cost shown; JS-side aggregation at current volume with a noted SQL-side path. See D-082. It works and is demoable and measured-honest, but is not yet delightful; it returns for a deliberate delight pass (a MEASURE-side polish pass, deferred).

**A4b (Insights cost + value/ROI lens) is DEFERRED** pending the business-model decision below: `cost_micro_usd` means the customer's spend under bring-your-own-model but legalOS's internal margin under managed pricing, so an honest cost/ROI lens needs the business-model/billing dimension first. A4b will also need the ROI assumptions (currently localStorage in the Productivity Calculator) moved to org storage.

**A6 (audit-log viewer) shipped (2026-06-01) — GOVERN COMPLETE.** A read-only People activity log under Govern presents the two trigger-written audit tables (`role_change_audit`, `user_status_audit`) as one chronological feed with a per-row event type: who changed whose role, and who deactivated/reactivated whom. Names are resolved via the batched lookup; null actor/target (deleted user or direct-SQL change) render as "the system" / "a former member", honestly revealing out-of-app changes. Org-admin gated (matches the tables' RLS), server-fetched with most-recent-N + load-more (a UNION view / keyset cursor and a multi-tenant composite index are noted as future, not built). No new recording, no migration. See D-083. The GOVERN side is now complete: Policy & access, People, Audit log.

**A7 (admin arc close-out) shipped (2026-06-01):** a docs-only reconciliation pass marking the arc's honest state (GOVERN complete, MEASURE deferred) and refreshing the handoff so a future session picks up cleanly.

Remaining MEASURE-side work, all intentionally deferred: **A4a delight pass** (the usage lens works but is not yet delightful); **A4b** (Insights cost/ROI, gated on the business-model arc, section 1a); **A5 Evals** — the deepest open design question ("what is an eval for a non-engineer?"), deferred for a real design conversation that has not started. The remaining legacy admin pages (Productivity Calculator, Adoption Metrics) were to stay reachable but unlinked, retiring once Insights fully covers their intent (after A4b); the calculator's half of that is superseded — it was rebuilt as the hybrid measured-plus-assumptions tool in the analytics arc (D-142) and joined the admin MEASURE navigation between Insights and Evals as "Productivity" (2026-06-09, D-147), fixing the gap where a real shipped tool was reachable only by direct link. Adoption Metrics remains unlinked, retiring once Insights fully covers it.

**PARKED operational item — invite-email sending domain.** The A3c invitation code is correct and complete, but real invites cannot SEND until a verified sending domain is configured in Resend and the Supabase custom-SMTP sender is changed from `onboarding@resend.dev` to an address on that domain. The operator has no legalOS domain yet and has chosen to defer testing. For a near-term demo: drive it on the operator's own account (no domain needed), or get a domain (~$12/yr + Resend domain verification) so demo users can be invited and log in. This is a Supabase/Resend configuration step, not an app bug. (A future session should treat this as parked pending a domain decision and stop resurfacing it; the over-permissioned Supabase MCP authorization a prior session requested was correctly declined and should not be granted.)

**Audit coverage expansion (future governance/security item):** event-log the currently-unaudited governance actions, Policy & access edits (capability ceiling, allowed categories, default model), connection grant/revoke, invitation lifecycle (resend/revoke/accept), and agent/department changes, and surface them in the audit log alongside the role/status events. Supports the security-transparency posture (security-questionnaire readiness). A6 surfaces only what is recorded today; this arc widens what is recorded.

**Security/governance transparency (standing lens, not a discrete arc):** design every governance/security decision to be cleanly articulable for landing-page / trust-center / FAQ / security-questionnaire use, and lean toward the more-explainable option at forks. This is a lens applied to all future work, not a milestone.

**Canonical home: `docs/SECURITY_ARCHITECTURE.md`** — the consolidated trust narrative (claim + architectural basis for each guarantee) now lives there as the single source the content phase draws from. The DECISION_LOG remains the raw material; SECURITY_ARCHITECTURE.md is the organized story. The accumulated claims it consolidates (least-privilege roles, soft-deactivation, encrypted credential isolation, bring-your-own-key, three-layer enforcement, audit trail, notify-and-approve, honest-state UI, the MCP trusted-only boundary, and the data-sovereignty story) are not duplicated here; update that document as each new trust-relevant decision ships.

## 1a. Business model & pricing (new major arc, after Connections — gates A4b)

Decide the pricing model before the Insights cost/ROI lens can be honest. Options: **managed** (we bring the models: platform + model + usage, tiered or marked-up), **bring-your-own-model** (the customer pays their own provider; we charge platform + support), or **both** (an emerging dominant pattern that fits the legal persona spectrum from solo to enterprise). Four pricing layers to decide which to expose: platform, model, usage, services. Cautions: inference costs decline roughly 10x/year, so build to be re-priced and don't hard-bake margins; heavier reasoning-model use offsets cheaper per-token rates. **Gates A4b:** `cost_micro_usd` is the customer's cost under BYO but legalOS's margin under managed, so the org needs a net-new business-model/billing dimension (BYO vs managed + plan price) before the ROI/cost lens is meaningful. Tightly coupled to the Connections "models as a connection" work (item 2), hence sequenced right after it.

## 1b. Platform-owner administration tier (major arc, after Connections + business model)

A SEPARATE administration tier ABOVE customer super_admins, with CROSS-TENANT scope, for legalOS-the-vendor (not a customer governing their own org). Distinct from everything built so far (Policy & access, People, audit log, the customer super_admin = all TENANT administration: a customer governing their own org). The platform tier is legalOS governing the whole platform across all customers, often a separate surface/console.

Three pillars:

1. **Customer/tenant management** — all customer orgs and all users from a 60,000-foot view; tenant lifecycle (provision/suspend/offboard); support tooling (incl. support impersonation, heavily audited).
2. **Commercial** — billing & subscriptions (this is where the business-model pricing decision is IMPLEMENTED and metered), account management, plan/entitlement management (what each tier unlocks).
3. **Platform operations** — cross-customer PRODUCT ANALYTICS for customer success/adoption (the platform cousin of tenant Insights/A4a; same `usage_events` data, cross-tenant scope, audience = legalOS for customer success/churn/adoption); C4L CONTENT MANAGEMENT (the legalOS-owner "ship this content version" approval + cross-org propagation; the C4L model in section 2 lands here); platform-wide model/connector CATALOG governance (which providers/models exist as OPTIONS platform-wide, where a new provider adapter is added and made available, vs. a customer ENABLING one at the tenant level); platform health/ops; cross-tenant security/audit (feeds the trust story).

Net-new concepts this introduces:

- Platform-STAFF identity — accounts not tied to any one customer org (a tier above per-org super_admins).
- DELIBERATE cross-org access — this INVERTS the org-scoped-RLS-everywhere safety the whole product is built on (a customer can only see their org). Platform admin is the one place ALLOWED to cross org boundaries, so it must be the most heavily gated and audited capability, and must be articulable in the trust story (legalOS-staff access to customer data is restricted, audited, scoped).
- A separate platform surface/console.

Tenant/platform SYMMETRY to design for: several capabilities have a tenant version (built) AND a platform version (future), Insights (their usage / all-customer usage), governance (their policy / platform options catalog), audit (their org's events / cross-tenant). Where possible share the underlying data/queries (e.g. `usage_events` powering both, scoped differently) rather than building twice.

Evals stays TENANT-level (customers evaluate their own agents, since customers drive agent content); C4L-content quality is the platform-level cousin.

Coupling: tightly tied to the business-model arc (billing implements the pricing model). Sequencing chain: Connections (models-as-a-connection) → business model (decide pricing) → platform tier (billing + the rest) → also unblocks A4b (the tenant cost/ROI lens).

**Org onboarding does not exist yet (parent work; agent-set seeding is a rider).** There is no org-creation/onboarding flow in the app today: organizations are born from deployment seed SQL (`supabase/seed/0001_org_and_departments.sql`), and no code path provisions a new tenant org or runs its baseline seeds. This lives under pillar 1 (tenant lifecycle: provision/suspend/offboard). When org onboarding is built, it should, as part of provisioning, SEED the baseline agent sets for the new org: the Claude for Legal library (via the existing per-org `importC4LContent` rails) AND the "Powered by legalOS" system tier (D-180). Until that onboarding flow exists, system-tier and C4L agents are seeded AD HOC to the deployment org (and copied into the demo org via `demo-org.sql`), and a future tenant created outside a deployment seed would not get them automatically. So: build org onboarding, and make baseline-agent-seeding a step within it, rather than a standalone seeding mechanism.

**Migration-workflow hardening — DONE (2026-06-25, D-192/D-193/D-194).** Database migrations are no longer applied to production by hand. The repo is now CLI-linked to Supabase, the migration ledger was baselined by marking the 76 historical `0001`..`0076` files as applied via `supabase migration repair --status applied` (the files are retained as canonical history; `db pull` was deliberately NOT used, which would have collapsed that granularity), and the inaugural tracked migration (`20260625163357_add_pre_step_result_to_messages`, the D-193 redline persistence column) was applied via `supabase db push` with `migration list` showing Local == Remote. Going forward: `supabase migration new <name>` -> edit -> `supabase db push`. The current path is CC-AUTHORS the migration files / OPERATOR-PUSHES them (the agent holds no credentials; the Supabase MCP stays read-only); CI-applied migrations (a deploy gate that runs `db push` automatically) remain a future option, not the present path. See `docs/MIGRATIONS.md` for the full workflow, including the operator's keychain-bypass CLI auth (env-var `SUPABASE_ACCESS_TOKEN` / `SUPABASE_DB_PASSWORD`) and the rotate-the-token / DB-password-was-reset follow-ups.

**Dogfooding pass — exercise the Knowledge + built-in-agents work on real documents, tune in place (standing, operator-driven, not blocking).** The full checklist lives in [`DOGFOODING.md`](../DOGFOODING.md) (Structured Query end to end, schema-grows-on-demand, extraction quality, the six built-in agents, Document Comparison, Research, and the empty-state deep-link); work through it opportunistically and check items off there. The summary below is retained for context. Run the built-in "Powered by legalOS" General Tools agents (D-181, D-187) against real documents, especially Document Comparison (the deterministic-engine flagship, D-185 through D-190), and tune the v1 system prompts where real use shows weakness. The tuning mechanism already exists and is cheap: edit the canonical prompts in `BUILTIN_AGENTS` (`lib/content/builtin-agents-seed.ts`) and re-run `npm run seed-builtin-agents`, which updates the live rows in place (the update-in-place pattern, D-181). Watch first: the PII Flagger's "review aid, not a guarantee" framing (does it stay appropriately humble and never imply completeness?) and the Obligations and Dates Extractor's deliberate lean toward over-inclusion (is it catching borderline items without drowning the signal?). The redline reload-persistence gap (D-189) has since been CLOSED (D-193): the change set is persisted on the assistant message (`messages.pre_step_result jsonb`, defensive selects) so the redline survives a reload; dogfooding now just confirms it holds on real comparisons. Standing item: operator-driven, picked up opportunistically, not blocking other work.

**Feedback Step Two — email notification of new feedback (DEFERRED until the company has a name and domain; standing, not blocking).** Step One shipped and is live (D-204): in-product submit from a quiet rail affordance, server-stamped auto-context, the platform-owner review/triage queue, and the calm unseen count. Step Two is the notification rail, and it is deliberately deferred until the company has a NAME and DOMAIN, so it is built ONCE with a proper branded sender rather than on a placeholder test domain (a branded "from" address is worth waiting for; a throwaway domain would be rework). The seam is already in place and untouched: the single `submitFeedback` insert action (`lib/actions/feedback.ts`) is where a fire-and-forget `notifyNewFeedback(feedback)` attaches, with the channel and sender as config (swappable, so Slack/in-app can replace or join email later). When resumed, Step Two = a Resend (or chosen provider) account + API key as a SECRET (operator-set in Vercel env, never hardcoded) + domain verification of the branded sender + the notification abstraction wired to the seam, with the recipient as config (default the platform owner's email). Until then, the live calm unseen count on the platform landing is the no-infrastructure visibility cue. Standing item: not blocking other work; resume once naming lands.

### C4L content library + platform-owner arc — build steps (IN PROGRESS)

The combined arc that makes the static C4L content library updatable + vendor-agnostic + governed, on a real platform-administration foundation. Five steps; the platform foundation (Step 1) lands first because the C4L refresh button and future platform features (billing, cross-tenant analytics) live on it. The cross-org propagation of C4L content remains deferred (theoretical while single-tenant; see the BUILD STAGING note under the C4L decided-model block in section 2).

- **Step 1 — platform_owner role + minimal platform-admin surface (SHIPPED, D-110).** A cross-tenant `platform_owner` capability modeled as a SEPARATE AXIS from the org `user_role` enum (a standalone, reassignable `platform_admins` grant, never self-grantable under RLS, migration 0058), gated by `isCurrentUserPlatformOwner()` / `requirePlatformOwner()`. A distinct, minimal platform-admin area at `/workspace/platform` (its own gated layout + a scalable card-style landing driven by an empty `PLATFORM_NAV_GROUPS`, mirroring the org admin shell one tier up), reachable via a "Platform" profile-menu entry + rail shown ONLY to a platform owner. No billing / analytics / cross-tenant management / C4L logic yet — just the role + gate + surface shell the later steps drop into. A mere org super_admin does NOT get access. **Operator: apply migration `0058_platform_admins.sql` and confirm the grant landed on steve@antinilaw.com.**
- **Step 2 — persist the C4L department mapping + make refresh filter-safe (SHIPPED, D-111).** The plugin→department placement (previously only in the operator's CLI `--department` args) is now version-controlled config in a vendor-content registry seam (`lib/content/vendor-registry.ts`, the C4L provider entry + `pluginDepartmentMap`, derived from live state). The import is refactored into a reusable, unit-tested server function (`lib/content/c4l-import.ts` — a pure `planC4LImport` core + an `importC4LContent` executor over an injectable store) that NEVER reactivates soft-deleted/curated rows, places new agents from the mapping, applies a conservative non-destructive update policy (existing rows are never overwritten; content drift is reported for later review), and reports unmapped plugins rather than guessing. Foundation only — no button, route, UI, or GitHub fetch (those are Step 3); code constant, no migration. The legacy one-shot CLI is superseded for refreshes.
- **Step 3 — the platform-owner C4L refresh button (SHIPPED, D-112).** On the platform Content library page (`/workspace/platform/content`, the first real `PLATFORM_NAV_GROUPS` area), a platform-owner-only "Refresh from source" button fetches the public C4L repo at runtime (`lib/content/c4l-fetch.ts`: 2 GitHub API calls — repo meta + one recursive git-tree — then raw.githubusercontent.com CDN reads for the mapped plugins' SKILL.md, so it stays well under the 60 req/hr unauthenticated limit; ignores `external_plugins`/`managed-agent-cookbooks`/non-plugin dirs via a strict `<plugin>/skills/<skill>/SKILL.md` path filter; never throws), parses with the same gray-matter parse the original import used (now a runtime dep), and feeds the result to the safe `importC4LContent` (Step 2). It REPORTS imported / skipped-filtered / unchanged / unmapped-upstream-plugins / content-updates-available — applying NONE of the last two (conservative: no auto-import of unmapped, no auto-overwrite of drift). Super admins cannot run it. The "push a button, don't re-code each update" capability, end to end, safely.
- **Quick fix (SHIPPED): rail/profile block shows platform_owner status.** The rail profile block now surfaces "Platform owner" when `isCurrentUserPlatformOwner()` is true (the Profile page in Settings already showed both org role and platform-owner status). Small cosmetic fix, done.
- **Step 4 — vendor-agnostic structure (SHIPPED, D-113).** The launchpad's single hardcoded external-agents bucket is now a registry-driven, source-split, dynamically-titled structure. `lib/content/vendor-registry.ts` is the single source of truth for vendor content providers (`lib/agents/source.ts` derives its labels/ids from it, no second list); `getAgentsForDepartmentLaunchpad` returns `externalGroups` split by `sourceId` (pure `groupAgentsBySource`, unit-tested); the launchpad renders one dynamically-titled `CollapsibleSection` per vendor present, with per-vendor collapse keys (the legacy Claude for Legal section keeps the `externalAgents` key so existing collapse preferences survive). Behavior-NEUTRAL with C4L as the only provider today; a future provider (e.g. an OpenAI legal library) is a registry entry + import adapter, not a rearchitecture. No migration (computed from existing `source_origin`).
- **Step 5 — super-admin governance + last-updated transparency (SHIPPED, D-114). THE C4L + PLATFORM-OWNER ARC IS COMPLETE.** A "Content" section in Policy & access lets a super admin turn each vendor content provider ON/OFF for the org; OFF genuinely hides that provider's launchpad sections org-wide (the launchpad gates on it), default PERMITTED. Backed by a per-`(org, provider)` `content_provider_settings` table (migration 0059) rather than the connection-policy permit-list — a deliberate choice (reported in D-114): a permit-list defaults to deny-when-absent, incompatible with vendor content's default-permit-with-explicit-deny, and a per-provider store is the multi-provider-ready shape the locked C4L-placement decision called for. A passive per-provider "last updated" line (written by the platform-owner refresh, read by super admins) gives honest update visibility with no adopt action (push model). **Operator: apply migration `0059_content_provider_settings.sql`.**
- **C4L + platform-owner arc — COMPLETE (2026-06-05).** Platform-owner tier (Step 1) → safe placement-preserving import + persisted mapping (Step 2) → one-button platform-owner refresh proven live (Step 3) → vendor-agnostic registry-driven structure (Step 4) → super-admin org governance + last-updated transparency (Step 5). Two-layer governance achieved: the platform owner owns what the content is; the super admin owns whether their org uses it. **The Workflows arc (the orchestration engine) is COMPLETE (2026-06-06).** Step 1 (2026-06-05, D-115): the headless `runAgent(agent, input) → { output, toolCalls, usage }` primitive. Step 2 (2026-06-05, D-116): the workflow data model (declarative step-graph definition + run/immutable-step-run audit records, migration 0060) and a pure, tested execution engine. Step 3 (2026-06-05, D-117): durable human-checkpoint pause/resume + approved-write execution + the run-level autonomy foundation (migration 0061). A run carries an `autonomy_level` (supervised default; autonomous represented); the engine pauses at checkpoints and before any write, persists a pending approval (token_ref only), and resumes on an owner decision via an atomic at-most-once claim. Step 4a (2026-06-05, D-118): the no-code builder + My Workflows surface — a form/list editor over the canonical graph (validated by the same engine validator before saving, capabilities resolved live from the agents + governed-MCP registries so new tools appear automatically). Step 4b (2026-06-06, D-119): run, watch, audit, and approve — start a run with run input + a run-level autonomy choice, follow the step-by-step run view (the immutable per-step audit trail with approval provenance), and approve/deny via a card mirroring the chat write-confirmation; UI only, over the existing `startWorkflowRun` / `decideWorkflowApproval` actions. The delight pass (D1-D3, D-121/2/3) made workflows agent-centric, and Step 5 (2026-06-06, D-124) shipped the Template Library + starter templates — **THE WORKFLOWS ARC IS COMPLETE** (see the detailed Workflows section below). (Triggers — scheduled/webhook — branching, and gated auto-pilot writes are later additive layers; manual run + manual approval are v1.) **Operator: migrations `0060`-`0063` apply in order; `npm run seed-workflow-templates` seeds the starters.** Since the Workflows arc, the following have ALSO shipped: the full public marketing surface (item 20, Tiers 1a-3 including the Legal document drafts, D-126 through D-135), demo access (item 21, D-132/D-133), the multi-tenant connection org-scoping security fix (D-136), and the item-6 code-health cleanup pass (the documentation truth pass D-137, mechanical hygiene D-138 which also closed item 16, and the targeted tests D-139). The Desk arc's personal-feeds half SHIPPED (2026-06-23, D-173: per-user RSS-by-URL feeds rendered as latest-item cards, migration 0075; with feed autodiscovery from any page URL and Apple Podcasts resolution, and the capability fully surfaced across the features tour, documentation, help drawer, support assistant, README, and claims map per the D-157/D-158 currency rule); its admin-curated role-scoped layer is the named additive follow-on. Google Calendar (read-only, today's schedule on the home Today card) SHIPPED (2026-06-23, D-174) as a sibling `google-calendar` OAuth adapter on Drive's direct-connection path (same client/callback/crypto/refresh/org-RLS, no migration); this deliberately took the direct-adapter path over the MCP-only framing the per-user-calendar arc note below had pencilled in (see D-174 for the supersession; the operator's Internal OAuth app removes the caution that motivated MCP-only). Still pending: Outlook calendar and the broader connection-system-unification / per-user-MCP work for other Workspace surfaces, and the Matters arc (both under item 2), the Desk's admin-curated layer; the Business model & pricing arc (1a) and the platform-owner administration tier (1b); and the lower-priority items further down this list.

### Analytics arc — build steps (COMPLETE, 2026-06-09)

The cross-customer analytics surface for the platform owner (the Platform operations pillar above), built on a reusable metric-layer framework so each new metric is turnkey. Guided by the measurement philosophy "measure what we value, not value what we can measure": adoption and engagement health first, restraint throughout.

- **Step 1 — metric-layer framework + the adoption/engagement-health hero (SHIPPED, D-140).** The legalOS-native metric layer: a metric is a named SQL VIEW (migration 0067), locked service-role-only via REVOKE/GRANT (it bypasses RLS by design to aggregate cross-tenant; `security_invoker` deliberately OFF), registered in `lib/platform/metrics/registry.ts` with string format tokens, read through the server-only admin client inside per-tile async server components behind `requirePlatformOwner()` (NO API route, NO client data hook — the RSC seam has nothing to leak through), and rendered by declarative primitives (`MetricTile`/`StatRow`/`Chart`/`Table`) drawn with hand-rolled SVG (no charting dependency). The first slice is the platform Analytics page (`/workspace/platform/analytics`): the per-customer adoption/engagement-health hero (the churn early-warning table — activation rate, usage trend, unused agents, last-active recency with a calm at-risk signal), an overall 30-day usage-pulse line, and a scalar summary where cost is shown (platform tier only). Demo orgs excluded; reader tolerates the pre-migration window (calm empty state, never a 500). Adding a metric is now a view + a registry line + a tile. **Operator: apply migration `0067_platform_analytics_views.sql`.**
- **Step 2 — cost + adoption funnels (SHIPPED, D-141).** The Cost group (spend-over-time, spend-by-customer, and a projected-monthly = trailing-30-day run rate; cost shown only at the platform tier) and the Adoption-funnels group (invite acceptance, connector adoption, demo-link conversion). Cost-by-model was deliberately held (an engineering curiosity, not a business-health signal). The page is organised into Engagement / Cost / Adoption groups with the health table as the centerpiece; `operator_demo_conversion` is the one view that is intentionally demo-focused (it measures the demo funnel, so it does not apply the demo exclusion). Migration 0068. **Operator: apply migration `0068_platform_analytics_cost_and_funnels.sql`.** With this the platform analytics are complete for now; remaining in the arc are the hybrid productivity calculator, the super-admin Insights reframe, and the marketing/README surfacing.
- **Productivity calculator Step A — DB-backed org task book + real-usage pre-fill + honest labeling (SHIPPED, D-142).** The manual localStorage calculator is now a hybrid tool: the run VOLUME is measured live from `usage_events` (per task type's mapped agent), salary and per-run time deltas stay human estimates, every number is marked measured vs. estimate, and the methodology modal explains the blend. The task book persists per org (migration 0069, super-admin write / org read), so admins share one book. Formulas unchanged; the measured volume now drives the result (annual hours = estimated per-run delta × measured runs). Migration 0069. **Operator: apply migration `0069_productivity_task_book.sql`.**
- **Productivity calculator Step B — individual Impact cells (SHIPPED, D-143).** The two dormant home-Impact cells ("Hours saved" / "Estimated cost saved") now light up per user: hours saved = the user's measured run volume (`usage_events`, the existing impact-math spine) × the org task book's estimated per-run time delta; cost saved = hours × the blended org-average rate. They respect the Week/Month/YTD toggle with a delta vs. the prior window, keep the honest "Setup needed" state until an admin configures the book (and an honest zero when the user has no runs), and carry a light "estimated" footnote. Reuses the Step-A compute; no new migration.
- **Presentation unification + Insights reframe (SHIPPED, D-144).** The shared metric primitives were moved to a neutral kit (`components/metrics/`, decoupled from the platform service-role read path), and org Insights and the home Impact card now re-render through them, so all three altitudes share one rendering system. Insights leads with adoption and engagement (active people, the runs trend, the adoption gap), drops the by-model breakdown (model/cost live at the platform tier), keeps cost withheld, preserves the sample-data toggle and the no-usage zero-state, and links to the Productivity Calculator. Presentation-only: data seams stay separate by altitude (service-role views at platform; RLS-scoped JS at org and individual), and all displayed numbers are unchanged. No migration.
- **Marketing + README surfacing (SHIPPED, D-145) — THE ANALYTICS ARC IS COMPLETE.** The marketing landing gains a measurement section, in the page's own voice and slotted into its entrance choreography, describing how legalOS shows a team the value it is getting: each person sees their own impact, leaders see how the team is adopting and using the product, and a built-in calculator estimates the return by combining measured usage with the team's own assumptions. The platform-owner cross-tenant analytics stay OFF the customer-facing page (internal operator tool), and the measured-vs-estimated line stays honest. The README reflects the measurement layer. Copy and documentation only; no migration. The arc shipped end to end: platform analytics (Steps 1-2), the hybrid calculator (Steps A-B), individual Impact, presentation unification, and the external surfacing. The data-capture arc (5a) remains the named follow-on for deeper measurement signals. (Follow-up, D-146: the landing measurement section was relocated to the /features product tour the day after shipping; a single capability reads off-brand as a section on the thesis page. The measurement story itself is unchanged.)

## 2. Connector follow-ups (deferred from the connector hub arc)

### Connections phase — framing (from the opening investigation)

The phase is not "extend the connector registry" but "generalize the connection abstraction beyond OAuth data sources." The current connector assumes one shape: a user OAuth-connects an external account whose data their agents read/write (the ProviderAdapter contract, the AES secret model, the category + read/write-ceiling governance are all OAuth-data-source-shaped). The next things to connect are different kinds of things, so the FOUNDATIONAL decision that gates the rest is: how to generalize the connection abstraction to support non-OAuth connection types (key-based model providers, MCP servers, governed-content sources). This is the first design conversation of the phase.

Findings that shape the work:

- Models-as-a-connection: the models.ts seam is clean and single-sourced (8 consumers derive from it, so swapping the source is contained, the easy ~20%). The surrounding ~80% is the real cost: Anthropic is an always-on platform env key (ANTHROPIC_API_KEY), not a connection, so BYO needs a connect path + per-org key resolution in the chat route; the connection abstraction is OAuth-only and does not fit key-based providers; pricing/cost has NO discovery source (a model discovered via a provider API has no price, pricing must be entered/configured); and a net-new per-model governance dimension (notify-and-approve) is needed that the read/write ceiling does not model.
- MCP: zero MCP in the codebase. The registry can host an MCP adapter alongside the OAuth one, but the adapter contract, connect flow, secret shape, and exercise path all need an MCP variant, a new mechanism, not a parameter. A significant reframe if pursued.
- C4L: today it is the import-c4l-plugin.ts script tagging agents rows with a source_origin string (no entity, table, provider, connection, live upstream, or governance). Reframing it as a connection is essentially net-new; the hybrid-edit "upstream-managed" semantics are a conceptual hook but there is no connection plumbing beneath them.
- Discovery automation: net-new infrastructure. No cron, scheduled functions, webhooks, background jobs, or notification system exist anywhere. The platform supports Vercel cron / Supabase scheduled functions (unconfigured); the discovery-and-notify loop stands up from scratch.
- Secret model: connection_secrets (AES-256-GCM, service-role-only) is reusable for a BYO key with no schema change (a sibling encryptApiKey is trivial), but the OAuth connect flow, TokenBundle typing, and refresh semantics do not carry over.
- Governance: notify-and-approve (LOCKED) maps onto a new "approved models" set the connection-derived list is filtered through, governed in Policy & access (which extends cleanly as a surface, but has no pending/approved-model concept and no notification primitive today, both net-new).

Suggested opening for the next session: the connection-abstraction design conversation (how to support non-OAuth connection types), since models-as-a-connection, MCP, and C4L all depend on it. Then models-as-a-connection (Anthropic first) as the first concrete build, which leads into the Business model & pricing arc, which unblocks A4b.

### Connections phase — BUILD (models-as-a-connection)

The phase's first concrete build, models-as-a-connection, is decomposed into four behavior-staged steps:

- **1a — generalize the adapter contract (SHIPPED, D-085).** The connector ProviderAdapter is now a discriminated union on a `kind` field, so the registry can host non-OAuth connection kinds (model providers next, MCP later) on one foundation. The OAuth-specific fields/methods moved under `kind: 'oauth'`; Google Drive is an oauth-kind adapter; the OAuth connect/callback routes narrow on kind before touching OAuth members. Pure abstraction refactor, zero behavior change, no schema change.
- **1b — Anthropic as a managed model connection (SHIPPED, D-086).** Added the `kind: 'model'` adapter variant and the Anthropic model adapter, and rewired the chat route's credential seam (the single `createAnthropicClient()` env-key read) through a server-side resolver. The route resolves the credential with the org/user/vendor context in scope and passes it down; in managed mode the resolver returns the same platform Anthropic key, so chat behavior is identical. The single platform-key read is centralized in the resolver; an unknown vendor does not resolve to Anthropic credentials. No schema, category, stored key, or UI in this step. No behavior change.
- **1c — BYO customer key (SHIPPED, D-087).** An org stores its own encrypted model-provider key (in the existing connection_secrets AES-256-GCM substrate via a thin encryptApiKey sibling), and the chat-route resolver's BYO branch decrypts and uses it instead of the platform key. A BYO model connection is an org-scoped connections row (capability_category 'models'), governed as its own Policy & access control (not the data-source policy gate). An explicit credential_source column (managed|byo) allows non-destructive switching; a nullable base_url column is added for future self-hosted; a unique partial index keeps at most one active org model connection per vendor. Keys are validated against the provider before storing, encrypted at rest, never returned or logged, shown only as a masked hint. Super-admin-gated save action (set/switch-to-managed/clear); grant-less, service-role resolution; OAuth token-refresh path untouched. Hand-applied migration 0051. Backend only.
- **1d — the connector UI (SHIPPED, D-088).** The "agnostic model connector" in Policy & access over the 1c actions: a super-admin control (read-only for other admins) on two legible axes, which provider (a provider-uniform grid with Anthropic available and Google, OpenAI, self-hosted coming soon) and whose credentials (managed vs bring your own Anthropic key). The Anthropic card shows the current state, a validate-before-store key entry with validating/success/friendly-error feedback (a bad key is never saved), a non-destructive managed↔BYO switch (key retained), and replace/remove (remove confirmed). The key is write-only, shown only as a masked hint, never re-displayed or logged. No base-URL field for Anthropic (fixed endpoint; surfaces for self-hosted later). Follow-up shipped: switchToBYO completes the non-destructive managed↔BYO round-trip (switch back to a retained key in one click, no re-entry; flips only when a usable retained secret exists).

**MODELS-AS-A-CONNECTION (flag 1: 1a-1d) is COMPLETE.** An organization can bring its own Anthropic key or use the managed default, configured in-product. BYO Anthropic is now usable end to end (the go-live proof-out's first connection).

### Connections phase — BUILD (MCP, flag 2)

MCP Phase 1 (the connection kind) is decomposed into three steps; Phase 2 (agent tool-use, the agentic loop) is a separate later arc.

- **2a — the MCP connection kind + the un-bypassable trusted-server registry (SHIPPED, D-089).** Added `kind: 'mcp'` to the connection adapter union and a code-level trusted-MCP registry (lib/connections/providers/mcp-registry.ts) that is the HARD CEILING on which servers can ever connect: only first-party servers registered in code, or customer-self-hosted endpoints via a partitioned path, are connectable. Trust is DERIVED from the registry (deriveMcpTrustTier), never stored as authority, so no row, admin action, or forged request can make an untrusted server appear trusted; the code allowlist can only be narrowed by per-org policy (2c), never widened. The boundary is established before any connect capability (the guarantee precedes the capability). Additive and behavior-neutral: no MCP client, connect flow, network, chat route, or schema change. Seeded with Google's official Workspace MCP servers (Drive, Gmail, Calendar, Docs, Sheets) as first-party entries with to-be-confirmed endpoints (finalized in 2b). Also consolidated the trust narrative into `docs/SECURITY_ARCHITECTURE.md`.
- **2b — the MCP client + OAuth 2.1 connect/auth + tools/list at connect.** Decomposed into three steps (the chat hot path stays untouched throughout Phase 1):
  - **2b-i — the MCP client capability (SHIPPED, D-090).** Added the standard @modelcontextprotocol/sdk (1.29.0) and a thin, isolated server-side MCP client (lib/connections/mcp/client.ts): given a server URL and an optional bearer token, it opens a Streamable HTTP connection, performs the handshake, and lists the server's tools (mapped to the 2a McpToolDescriptor); a tools/call wrapper is defined for later. Token-safe errors, timeouts, always-dispose. Fully isolated — nothing imports it yet; no connect flow, OAuth, chat route, UI, or schema change. The optional token is the seam where 2b-ii's OAuth token attaches.
  - **2b-ii — the OAuth 2.1 connect/auth flow + trust-gated connection.** Approach: Option A (use the @modelcontextprotocol/sdk's discrete step-functions — discovery, registration, exchange, refresh — which return secrets to us, and orchestrate via our own routes/crypto/tokens so custody stays in our encrypted substrate; the control-plane principle). Decomposed into three steps (the chat hot path stays untouched):
    - **2b-ii-1 — generalize token-refresh dispatch by connection kind (SHIPPED, D-091).** tokens.ts now resolves the refresh strategy from the connection's kind: OAuth-kind resolves its adapter via the registry and refreshes exactly as before (Drive byte-for-byte unchanged, and the prior hardcoded-Drive coupling is resolved); MCP-kind refresh is a marked placeholder, unreachable until the MCP connect flow exists. Behavior-neutral; no schema change.
    - **2b-ii-2 — the first-party MCP connect/auth flow (SHIPPED, D-092).** An MCP auth module (lib/connections/mcp/auth.ts) drives the SDK's discrete step-functions (discovery, RFC 7591 dynamic client registration, startAuthorization, exchangeAuthorization, refreshAuthorization) over discovered endpoints, with separate MCP connect/callback routes that store every secret — the OAuth tokens AND the registered-client info — in our encrypted connection_secrets (the SDK never custodies). Two-point trust gate (isTrustedFirstPartyServer at initiate; deriveMcpTrustTier must be first_party at callback) makes an untrusted server create nothing. The MCP refresh branch (2b-ii-1 placeholder) is filled. Org-scoped, super-admin-gated, grant-less; the RFC 8707 resource/audience seam is open. First-party only; the Drive OAuth flow is untouched (separate routes). Correct-by-construction against the SDK's typed contract; live proof deferred until a real server exists. No migration (reuses base_url + capability_category='mcp').
    - **2b-ii-3 — the self-hosted partitioned path (SHIPPED, D-093).** A separate self-hosted initiate route (super-admin, https-validated customer URL) derives a reserved `self-hosted:<origin>` server id (disjoint from registry ids), trust tier 'self_hosted' derived from the path, carried in the signed state; the shared callback re-derives the tier from the id namespace (registry-wins) and asserts it, so the two paths can never cross. Reuses the 2b-ii-2 discovery-backed OAuth machinery and custody (tokens + client info in our encrypted vault). Grant-less, org-scoped; first-party behavior unchanged. Both trusted MCP tiers (first-party official + customer-self-hosted) now connect. No migration.
  - **2b-iii — tools/list-at-connect + store (SHIPPED, D-094).** At the end of a successful connect (both tiers), best-effort call listMcpServerTools with the just-exchanged token and store the catalog in a new nullable jsonb column connections.discovered_tools (hand-applied migration 0052). A discovery failure does not fail the connection (best-effort, like account-label fetch); the catalog reads instantly for display (no per-render network) and is the substrate a future refresh and flag-4 discovery compare against. A read helper (getOrgMcpConnections) exposes the connection identity, DERIVED trust tier (never stored), status, and catalog for the UI. The MCP connect story is complete: both trusted tiers connect and record their tools.
- **2c — the connector UI (SHIPPED, D-095).** The MCP connections control in Policy & access (super-admin-interactive, read-only for others): two visibly-trusted connect paths — the first-party servers from the code allowlist (shown honestly, with a not-yet-configured state while the real Google endpoints + a Google Cloud OAuth client are pending) and a self-hosted https-URL form for the org's own server. Connected servers render from getOrgMcpConnections with a DERIVED trust tier shown as a clear label, status, and the discovered tools; disconnect is super-admin with confirmation. The trusted-only posture is visible by construction (no add-any-server affordance). The connect routes now return to the admin page. Formal per-org allowlist-narrowing is deferred (connecting is the enabling act; the code allowlist stays the hard ceiling).

**MCP PHASE 1 (flag 2: 2a-2c) is COMPLETE.** The trusted-only MCP connection capability ships end to end: connect a first-party or self-hosted MCP server under super-admin governance, with legalOS-owned credential custody and the tool catalog discovered at connect, the trust posture visible in the UI. **Next: Phase 2** (the agent tool-use loop — per-org tool resolution into the chat route, the multi-turn agentic loop, per-call confirmation, tracing, Docs/Sheets export) as the separate arc. **Go-live proof-out:** BYO Anthropic done; Google Workspace via MCP once endpoints + a Google Cloud OAuth client are configured (the first-party servers light up then), else OAuth. Deferred refinement: formal per-org MCP allowlist-narrowing.

- **Static (pre-registered) OAuth client support for DCR-less MCP servers (SHIPPED, D-097).** Groundwork that closes the client half of Google Workspace enablement: the trusted-MCP registry now declares per server how its OAuth client is obtained — dynamic registration (default; self-hosted and any DCR-capable server, unchanged) or a `static` client pre-registered out of band, whose id/secret the flow reads from a dedicated server-only env pair (`GOOGLE_MCP_OAUTH_CLIENT_ID` / `GOOGLE_MCP_OAUTH_CLIENT_SECRET`, deliberately separate from the Drive connector's). The Google entries are `static`; unconfigured creds fail the connect cleanly with a specific message; the static client info flows into the same encrypted custody path, so refresh is identical. Additive, behavior-neutral for the dynamic path, no migration. (Discovery endpoints remain the to-be-confirmed placeholders, finalized separately; this supplies the client, not the endpoints.)

- **Real Google Workspace MCP endpoints wired; registry aligned to the three offered servers (SHIPPED, D-098).** Replaced the placeholder Google endpoints with Google's official global MCP server URLs for Drive, Gmail, and Calendar (confirmed verbatim from Google's own documentation/console), keeping their pre-registered static OAuth client. Removed the Docs and Sheets entries — Google offers no dedicated MCP servers for them, and the trusted allowlist must reflect only real, vetted servers (Drive's create_file covers creating those files). The three now report `configured=true` and render as connectable in the connector UI. Registry data only; no auth/route/trust/custody change, no migration. **The go-live proof-out is now actionable:** the `GOOGLE_MCP_OAUTH` creds are set in Vercel, so a redeploy + migration 0052 (tool-catalog persistence) makes Drive/Gmail/Calendar connectable live from Policy & access. Phase 2 (agent tool-use) is the next arc once connect is proven live.

- **Per-server OAuth scopes + forced account selection for static MCP servers (SHIPPED, D-099).** Added the OAuth scopes each Google server requires (Google's documented scopes: Drive `drive.readonly`/`drive.file`, Gmail `gmail.readonly`/`gmail.compose`, Calendar the three calendar scopes) to the trusted registry, and the static-client auth request now declares them as the OAuth `scope` parameter, resolving the live-connect "Missing required parameter: scope" error. Also added `prompt=select_account` so the Google account chooser always appears. Dynamic/self-hosted (no declared scopes) unchanged; no endpoint/trust/custody/route/migration change. **Remaining for the live Google connect:** add the same scopes to the Google Cloud consent screen's Data Access page; authorize with a Workspace (antinilaw.com) account (the Internal consent screen requires an org account); apply migration 0052 for tool-catalog persistence. Then Phase 2 (agent tool-use).

#### MCP Phase 2 — agent tool-use (the gated agentic loop)

Phase 1 connects MCP servers and discovers tools (proven live). Phase 2 lets agents USE those tools mid-conversation via a gated loop on the chat hot path. A read-only investigation mapped the hot path (strictly single-pass today; web_search is a hosted tool needing no loop, MCP tools are client-executed and need a model→execute→feed-back loop) and a 7-step decomposition. **The design is locked in `docs/PHASE2_MCP_TOOLUSE.md` (D-100):** server-prefixed tool namespacing with a routing map; two-layer governance (org connects a server, the agent author enables which connected servers an agent may use, per-server in v1); v1 runs read tools and blocks write tools pending a later confirmation step, classified by captured MCP annotations; an 8-round + ~240s wall-clock guard; one summed `usage_events` row per turn plus an `mcp_tool_call_count` column; tracing via the existing `tool_calls` shape. Off-hot-path steps land first; the one hot-path change (the loop) lands last, gated and flagged, so no-MCP agents stay byte-identical.

- **2P-0 — design lock (SHIPPED, D-100).** The locked spec above, recorded so every subsequent step builds to a fixed design.
- **2P-1 — execution-resolution reader (SHIPPED).** An internal reader (`getOrgMcpExecutionTargets`) that resolves the org's active MCP connections to `{ serverId, connectionId, tokenRef, serverUrl, derived trustTier, tools }` — the connection id + token reference `getOrgMcpConnections` doesn't expose, which the loop needs to mint a token and call the server. Service-role read, scoped to the platform's single-tenant org boundary (the `connections` table has no `organization_id` column yet; the multi-tenant org filter lands with that schema). Pure addition; unused until 2P-6; no migration, no hot-path change.
- **2P-2 — tool mapping + namespacing (SHIPPED).** A pure module (`lib/connections/mcp/tool-mapping.ts`) that turns the 2P-1 execution targets into (a) Anthropic custom-tool definitions and (b) a routing map (namespaced name → `{ serverId, connectionId, tokenRef, serverUrl, originalToolName }`). Server-prefixed names `<prefix>__<tool>` (`gdrive`/`gmail`/`gcal` for the Google servers; a sanitized-slug-plus-stable-hash derivation for any other/self-hosted server) that satisfy Anthropic's `^[A-Za-z0-9_-]{1,64}$` with deterministic sanitization, a truncate-plus-hash scheme for the 64-char cap, and a last-resort uniqueness guard. Defensive `input_schema` normalization degrades malformed/missing schemas to `{ type: "object", properties: {} }` and never throws. Also widened the `AnthropicTool` type to a union (the unchanged hosted web_search variant + the new custom-tool variant). Pure, unit-testable; nothing in the chat route uses it. No migration, no hot-path change, no behavior change.
- **2P-3 — single-tool execution (SHIPPED).** `executeMcpTool` (`lib/connections/mcp/execute-tool.ts`): given a 2P-2 routing entry + the model's tool input + the tool_use id, it resolves a fresh token via `getUsableAccessToken` (MCP-aware refresh, custody ours), calls the server via the Phase 1 `callMcpServerTool`, and shapes the outcome into an Anthropic `tool_result` (text/structured success, size-capped at ~25k chars with a truncation note; or `is_error: true` with a token/PII-safe, model-recoverable message). It NEVER throws (one bad call can't crash a turn) and returns a token/PII-free trace record for 2P-6 to persist into `tool_calls`. Reuses `callMcpServerTool` + `getUsableAccessToken` unchanged; nothing in the chat route calls it. No migration, no hot-path change, no behavior change.
- **2P-4 — read/write annotation capture + classifier (SHIPPED).** `listMcpServerTools` now captures each tool's MCP annotations (`readOnlyHint`/`destructiveHint`/`idempotentHint`/`openWorldHint`/`title`, previously dropped) into a new OPTIONAL `annotations` field on `McpToolDescriptor` — additive, riding the existing `discovered_tools` jsonb with no migration (older annotation-less catalogs stay valid). A pure classifier (`lib/connections/mcp/tool-classification.ts`) marks a tool `read` ONLY when affirmatively read-only and non-destructive, and `write` in every other case including absent annotations (the locked conservative default). Discovery is otherwise behavior-neutral; nothing in the chat route enforces this yet. No migration, no hot-path change.
- **2P-4b — refresh an already-connected server's tools (SHIPPED).** A super-admin action (`refreshMcpServerTools`, alongside `disconnectMcpServer`) + a per-connected-server "Refresh tools" control in the connector UI that re-runs discovery (now capturing annotations) and best-effort updates `connections.discovered_tools`. Reuses `getUsableAccessToken` + `listMcpServerTools` (custody ours). BEST-EFFORT + NON-DESTRUCTIVE: a failed refresh leaves the existing catalog intact and returns a token/PII-safe error (never wipes a good catalog). This backfills hints for the Google servers (discovered before 2P-4). Explicit/manual only — no scheduled refresh; it is also the seam flag 4 (discovery automation) will reuse. No migration (annotations ride the existing `discovered_tools` jsonb), no chat-route change.
- **MCP token-refresh fix (SHIPPED, D-102) — prerequisite for 2P-6.** The MCP OAuth request now asks for offline access (`access_type=offline` + `prompt=select_account consent`), so a refresh token is issued and stored and expired access tokens renew automatically (the root cause behind "refresh fails on all three" and connections appearing to vanish). A lapsed connection (`status='error'`) is now surfaced in the connector with a "needs reconnect" state + Reconnect action instead of being hidden (display reader includes error rows; the execution reader stays active-only). The manual tool refresh reports an honest typed reason and no longer flips a connection to error as a side effect (reserved for runtime token death, the default `markErrorOnFailure` path). Disconnect unchanged; no migration. **Operator action: reconnect the three Google servers once** to gain a refresh token (they were connected before this fix).
- **2P-5 — per-agent governance (SHIPPED then SUPERSEDED).** Originally shipped per-agent MCP enablement (D-103); **reversed and replaced by org-level governance (D-104)** — see below.
- **MCP-to-agent governance is org-level (SHIPPED, D-104, supersedes 2P-5/D-103).** Removed the per-agent `enabled_mcp_servers` layer (column dropped, migration 0054; agent form MCP-free again) because basic users author their own agents, so per-agent admin control is impractical. MCP access is now governed entirely org-level by the super admin via two existing levers that BOTH must agree: (1) the **Allowed-connections policy permits the `'mcp'` category** — now a governed category (`KNOWN_CATEGORY_IDS` gains `'mcp'`; the Allowed-connections control offers an MCP toggle; **default permitted**, migration 0055), and (2) the server is **connected and healthy**. The org-level resolver `resolveOrgMcpTools` (`lib/connections/mcp/agent-tools.ts`) intersects `isCategoryAllowed('mcp')` with `getOrgMcpExecutionTargets` (active-only) and returns `{ targets, toolDefs, routingMap }`. Denying the category or disconnecting a server instantly removes the tools from every agent; no per-agent grants, nothing for basic users to configure. Nothing in the chat route calls the resolver yet. **Operator: apply migrations 0054 (drop column) and 0055 (govern + permit `'mcp'`).**
- **2P-6a — test foundation (SHIPPED).** Added vitest (minimal node-env config; `@/*` + `server-only`/`client-only` aliases; tests are `*.test.ts`, never in the route import graph so the Next bundle is unaffected) and 30 unit tests for the building blocks the loop depends on: 2P-2 tool mapping / server-prefix namespacing (charset, 64-char cap, uniqueness) / `input_schema` normalization / routing map, 2P-3 result + error shaping (text, structured, tool-reported error, truncation, token/PII-safe error messages), the 2P-4 read/write classifier, and the security-critical `deriveMcpTrustTier` (registry-wins, self-hosted, and never first_party for a forged id). `npm run test` runs them. The only runtime change was a behavior-neutral extraction of the 2P-3 shaping into exported pure functions (`shapeToolResult` / `shapeToolError`); the build, lint, and all feature behavior are unchanged. Fulfills D-101.
- **2P-6b — the gated agentic loop (SHIPPED, D-105). Phase 2 is functionally complete behind a flag.** A multi-turn tool-use loop in the chat route, DOUBLE-GATED: an env flag `MCP_AGENT_TOOLS_ENABLED` (default OFF — off ⇒ byte-identical single-pass path, no extra DB read) AND the has-tools gate (`resolveOrgMcpTools` non-empty). When engaged, the model can call connected MCP READ tools (`executeMcpTool`), results feed back, and it continues until it answers; WRITE tools are held with a needs-confirmation result (v1 — nothing sent/created/deleted). Guards: ≤8 model rounds + ~240s wall-clock, then a forced final no-tools turn. `chat.ts` accepts content-block messages + exposes `finalMessage`; usage is summed across rounds into one `usage_events` row + `mcp_tool_call_count` (migration 0056). History persists as final assistant text + `tool_calls` JSONB (content-block tool turns are ephemeral), so replay stays uncorrupted. web_search works alone and alongside MCP tools. The 30 2P-6a unit tests still pass. **Operator: apply migration 0056 and set `MCP_AGENT_TOOLS_ENABLED=true` to test live.**
- **Phase 2 agentic loop proven end-to-end to the final external gate (D-106).** The loop runs multi-turn against live Google MCP servers, tools resolve under org governance, tokens refresh, calls execute over the live transport (HTTP 200), and traces persist — all verified. The one remaining blocker is **external**: Google's Drive read tools return "The caller does not have permission" despite correct args, granted scopes (`drive.readonly`+`drive.file`), a trusted dedicated MCP OAuth client, the Workspace account, and the base API enabled; Google returns only the bare one-liner with no structured reason. Traced to a Google-side Workspace **Developer-Preview authorization gate**, not a legalOS defect.
- **RESOLVED (2026-06-04) — Google Workspace Developer-Preview Drive MCP data-access gate (D-106).** Root cause: Google Cloud project 738626459610 (legalOS-dev) was not enrolled in the Google Workspace Developer Preview Program. Enrollment was submitted and confirmed 2026-06-04; Drive MCP reads were then verified working live end-to-end against real Drive data. Every legalOS-side configuration was correct throughout; the sole blocker was the missing preview enrollment. **Phase 2 is now fully proven live.**
- **Open item (small) — Calendar MCP reconnect — effectively SUPERSEDED.** Calendar is connected and working at the org level now; the small fresh-consent reconnect wrinkle is no longer the forward path. The real next step for Calendar is the **Per-user calendar (via MCP) arc** below — surfacing each user's OWN calendar on their home — which subsumes this item.
- **2P-7a — friendly MCP tool-trace UI (SHIPPED).** Each MCP tool call now shows a human-friendly label ("<Server>: <action>", e.g. "Google Drive: search files") with calm running, done, held (needs-confirmation), and error states, surfacing the safe `error_message` reason on failure, in the same understated register as the web search indicator. Presentation only, rendered from the trace the loop already persists; no change to the chat route, governance, gating, or what's sent to any server, and no migration. Labels derive from the namespaced tool name (`lib/chat/tool-display.ts`, unit-tested) so they read the same mid-stream and after a reload, with graceful fallback for unknown/self-hosted prefixes.
- **2P-7b-i — pause-and-resume write-confirmation (SHIPPED, D-107).** A write tool now PAUSES the agentic loop instead of silently blocking it: the resumable loop state + the pending action persist to a tenant-scoped `mcp_paused_runs` table (a `token_ref` pointer only, never a token), a `tool_confirmation_required` event surfaces an Approve/Deny card, and the request ends cleanly — no serverless function is held open while a person decides. A separate owner-authorized decision (`POST /api/chat/confirm`) records the choice and resumes the loop in a fresh request, reusing the streaming machinery now extracted to `lib/chat/assistant-stream.ts`. Deny is fully wired (the agent continues and acknowledges the decline); approve is recorded and resumed with a placeholder (the write itself executes in 2P-7b-ii, so no write fires yet and the v1 block is preserved). Reads still execute inline; flag-off / no-MCP requests are byte-identical; history stays string text + `tool_calls` JSONB with the content-block loop state kept only in the paused-run record. **Operator: apply migration `0057_mcp_paused_runs.sql`.**
- **2P-7b-ii — approved writes execute (SHIPPED, D-108). PHASE 2 COMPLETE.** On an owner-approved resume, the held write now actually runs through the same `executeMcpTool` reads use, with a fresh token resolved live from the persisted `token_ref` (never a stored token), the real result fed back, and the loop continuing so the agent reports what it did; the trace settles to the real outcome (`done` | `error`, with the safe reason on failure), so the friendly UI shows e.g. "Google Drive: create file · Done". Deny is unchanged; the atomic `pending → resuming` claim guarantees the write fires at most once on a double-click/retry. A small, surgical change to the approve branch only — no schema/migration, no change to the pause mechanism, auth, UI, reads, governance, gating, or the flag-off path. **The full agentic MCP tool-use loop is now complete and proven live: governed reads run freely, writes run only with explicit per-action human approval.**
- **Phase 2 — COMPLETE (2026-06-04).** Connect a trusted MCP server under org governance (Phase 1), and agents use it mid-conversation: read tools auto-run, write tools pause for per-action human Approve/Deny, with the friendly trace UI throughout. Remaining later refinements: batch "approve all" (per-action only today) and abandoned-paused-run usage accounting (D-107). **Operator follow-up (small):** confirm whether `MCP_AGENT_TOOLS_ENABLED` is set to `true` in production. The gate defaults OFF (flag-off ⇒ byte-identical single-pass chat); agent MCP tool-use is live only when it is on, so verify the live value matches the intended state. **Next major arc is the operator's choice** — the Connections phase (models-as-a-connection, the notify-and-approve lifecycle), which gates the Business model & pricing arc and then A4b; or the platform-owner administration tier (Flags 3/4). See the Connector follow-ups section below and `docs/CHATBOT_HANDOFF.md`.

- **Customer onboarding for Google Workspace MCP (avoid the preview-enrollment trap) — KNOWN UX PRIORITY before external customers connect Google MCP.** Connecting Google Workspace MCP requires several Google-side steps that are easy to miss and that produced a long, opaque failure for us (D-106): the Cloud project must be enrolled in the Google Workspace Developer Preview Program (keyed by Cloud project number), the OAuth client and consent screen must declare the right Data Access scopes, the app must be trusted in Admin API controls, the base and MCP APIs must be enabled, and the authorizing account must be a Workspace domain account (not a personal Gmail). None of these surface a clear error when missing; the symptom was a bare "The caller does not have permission" with no actionable reason. **The need:** a delightful, guided onboarding flow so a future customer (or our onboarding engineer) never has to debug this blind. Directions to explore later (do NOT build now): (1) a guided setup checklist/wizard in the connector UI that walks the admin through each Google-side step with links and validation; (2) a pre-flight diagnostic that checks each prerequisite (enrollment, scopes, trust, APIs, account type) and reports exactly what's missing in plain language; (3) an intake form capturing the exact info we need from the customer (Cloud project number, Workspace domain, admin account) to do or verify the setup for them; (4) precise step-by-step documentation. Roadmap note only.

- **Connector catalog pre-seeded from C4L (SHIPPED, 2026-06-10, D-150).** The 13 `.mcp.json` connector configs in Anthropic's claude-for-legal repo (upstream HEAD `248331e`) were harvested into the trusted-MCP registry as a pre-vetted catalog: 18 distinct connectors (deduped across plugins; Google Drive reconciled against the existing verified entry at the same endpoint; CoCounsel deferred until a customer brings a Thomson Reuters subscription) across contract lifecycle, document management, e-discovery, court data, legal research, outside counsel, skill libraries, and productivity. Every entry is disabled by default (registry membership only; an org's super admin connects it through the existing governed flow with the org's own credentials) and carries endpoint, auth model, category, practice areas, provenance with the upstream commit, and an honest AVAILABLE/VERIFIED status (verified = legalOS proved connect + tool discovery + a real agent read; the three Google servers qualify per D-106). Surfaces: a platform-owner Connectors page (`/workspace/platform/connectors`, in the platform nav) shows the catalog with status and provenance; the org's MCP connections control in Policy & access lists the entries grouped by category with honest access notes ("Requires an Ironclad account.") and a note when the org's policy denies the `mcp` category. The C4L GAP ANALYSIS IS COMPLETE with this: agents fully covered (all nine mapped plugins imported), connectors harvested here, the skill library deferred (item 3), and the upstream commit now recorded at import/refresh time (vendor-registry `upstreamCommit`, catalog source constant, and the refresh report's source-commit line) so future gap analyses need no git archaeology. **Operator: CourtListener live verification (~3 minutes).** Everything up to the human consent step is proven against the live server (OAuth discovery, dynamic client registration HTTP 201, and the authorize request accepted and redirecting to CourtListener sign-in); the consent itself needs a person: (1) in production Policy & access → MCP connections → Court data → CourtListener → Connect; (2) sign in with a free courtlistener.com account and approve; (3) confirm the row shows "Connected · N tools"; (4) ask a litigation agent something docket-shaped (e.g. recent filings in a federal case) and confirm the "CourtListener: …" trace executes; (5) report back, and the catalog status flips to VERIFIED (a one-line change in `lib/connections/providers/c4l-connector-catalog.ts`). If the consent screen ever errors about scopes, the registry entry's `scopes` field is the one-line fix (the server supports `openid` and `api`).

- **Connector drift detection in the platform refresh (SHIPPED, 2026-06-11, D-151).** The refresh now also reads the upstream `.mcp.json` configs and diffs them against the shipped catalog, reporting added/removed/changed connectors notify-and-review style (endpoint changes flagged prominently; a change to a verified connector flags re-verification; removed connectors reported, never auto-removed). Never auto-applied — the catalog is the compiled-in trust ceiling (D-089). Partially serves the Sync-pipeline-Shape-B backlog item.

### Documentation arc — build steps (IN PROGRESS)

Self-serve product documentation as a trust asset: public (evaluators read docs before requesting access), role-structured, tour-depth, honest-state, with the currency rule keeping it true.

- **Step 1 — the public documentation (SHIPPED, 2026-06-12, D-158).** Thirteen guides on the marketing shell replace the Tier-2 coming-soon shell: six for everyone (getting started, workspace, chat, workflows, knowledge, impact) and seven for administrators (people, policy, connections, collections, workflows administration, insights, audit) — admin docs deliberately public; platform-owner material internal-only in `docs/OPERATOR.md` (the operator runbook index). The D-157 currency rule extended: user-facing behavior changes reconcile their documentation page(s) in the same commit. Screenshots deferred (rot risk).
- **Step 2 — in-product help links (SHIPPED, 2026-06-12).** A quiet, consistent "Help" affordance in every mapped workspace surface's header deep-links to that surface's documentation guide in a new tab: the launchpad and chat, the workflows surfaces (run surfaces to the user guide, builder surfaces to the administration guide), Knowledge/Collections/Research, the home Impact card, and each admin surface to its matching admin guide (the Collections surface role-conditions between the managing guide and the user guide; the MCP connections section inside Policy & access carries its own link to the connections guide). One declarative topic→guide map (`lib/workspace/help-links.ts`) feeds a shared server-safe `<HelpLink>`; the topic union makes a dead link a compile error, and a test pins every href to a published guide slug, so a guide rename is one edit. Platform-tier surfaces carry NO public help link by design (their documentation is `docs/OPERATOR.md`).
- **Step 3a — the support page (SHIPPED, 2026-06-12, D-159).** /support is a calm routing hub on the shared marketing shell: documentation as the primary route (surfaced by audience, deep-linking the hub's two group anchors), contact as the human path. Composed as independent sections so the assistant joins as a sibling section later without redesign, and deliberately unmentioned in the copy until it ships (honest-state, the features-page video-scaffold discipline). The footer's Resources column now reads Trust, Documentation, Support, FAQ. The Blog shell retired in the same change (see the marketing item).
- **Help drawer (SHIPPED, 2026-06-12, D-162).** The in-workspace Help links now slide in a right-side drawer rendering the mapped guide in place (the same lib/marketing/documentation.tsx module the marketing site renders — one source, two surfaces, no drift); the new-tab journey is the drawer's explicit "Open in documentation" escape.
- **Step 3b — the support assistant (BUILT TO PREVIEW, 2026-06-12, D-160; the public flip is the remaining gate).** Docs-in-context, not retrieval: the entire public documentation corpus (~17k chars from lib/marketing/documentation.tsx, rendered to plain text) rides in the system prompt on every call (prompt-cached), so grounding is total. Haiku 4.5 for speed (measured 1.4-5.7s per answer in live smoke tests, warm answers ~$0.0007); citations are model-emitted slugs resolved against the real guides (hallucinated slugs drop, never a dead link); legal-advice bait, off-docs questions, and roadmap/pricing speculation all decline honestly with a path forward (verified live). Guardrails ship from day one: 10 messages/minute per caller, 2,000-char messages, a 1,000-message global daily cap that fails calm ("the assistant is resting"), anonymous client-held sessions with nothing persisted. Spend is ledgered in `support_usage_events` (migration 0073: the customer ledger's NOT NULL org/user attribution is right to keep, so anonymous support spend gets a service-role-only sibling table). It lives as a SILENT floating corner launcher on every public marketing page (D-161; mounted once in the (marketing) layout, so the workspace structurally cannot carry it; never auto-opens, never animates for attention, with that silence recorded as a design rule in the component); /support carries a quiet pointer to the corner instead of an embedded panel. Still PLATFORM OWNER ONLY (the launcher gates itself via GET /api/support, so marketing pages stay static and the landing choreography is untouched); **the public flip is one line: `SUPPORT_ASSISTANT_PUBLIC` in `lib/support/config.ts`**, decided by the operator's delight verdict after beating on it (real questions, adversarial questions, off-docs questions, legal-advice bait). **Operator: apply migration `0073_support_usage_events.sql`** (the route answers fine without it; spend just goes unledgered until then).

#### Future arcs identified during Connections-phase planning (2026-06-05)

During Connections-phase planning we read the workspace-home surfaces (the "Today" calendar card, "Matters", and "Desk") against the live MCP backend and decided three substantial features each warrant their OWN arc rather than being folded into connections polish. Recorded here so the intent survives.

- **ARC — Per-user calendar. SUPERSEDED for the read-only Today card by D-174 (2026-06-23): the user's own Google Calendar now surfaces on the Today card via a DIRECT `google-calendar` OAuth adapter (per-user `scope='personal'`), not MCP.** The note below captured the earlier MCP-only intent; it is kept for the reasoning, but the read-only personal-calendar surface shipped on the direct-adapter path (the operator's Internal OAuth app removed the dual-path caution). What remains genuinely open from this note is the broader per-user dimension of the MCP model for OTHER surfaces (agent tool-use over calendar/Gmail), not the Today card. Original intent follows. Each user's OWN Google Calendar surfaces on their workspace home: the "Today" card shows THEIR schedule, not a shared one. **(Original plan) Built on the MCP architecture — MCP remains the only Google/Workspace path** (no separate calendar-OAuth connector; this deliberately avoids re-introducing the dual-path entanglement reasoning rejected in the org-level-MCP-governance decision, D-104). The core work is **adding a PER-USER dimension to the MCP connection model**, which is org-scoped only today (`connections` rows are `scope='org'`, `owner_user_id=null`; resolved org-wide via `getOrgMcpExecutionTargets`). Per-user calendar is the FIRST case of that capability, and the per-user-MCP foundation will be **reusable for other per-user Workspace connections later** (per-user Gmail, Drive, etc.).
  - **Governance (DECIDED, D-109): admin-controlled, the user has NO say.** If the super admin enables/connects per-user calendar for the org, it connects for users — the user does not opt in or choose. The super admin's decision IS the grant, consistent with the org-level MCP governance pattern (admin sets it; proper use is policy/training, not a per-user toggle).
  - **Experience goal (DECIDED):** delightful for BOTH the end user (their real calendar, effortlessly present on their home) AND the super admin / platform owner (a simple, clear control to turn it on — not a fiddly per-user setup).
  - **Current state for context:** the calendar card (`components/workspace/home/calendar-connect-card.tsx`) and the rich `today-schedule.tsx` view are already built but DORMANT behind the Reading-1 gating pattern (D-057/D-058). The card currently checks a per-user `'calendar'` category via `connection_grants` — a path that **never shipped** and is **blind to the org-level MCP Calendar connection** (category `'mcp'`, grant-less), which is exactly why it still reads "NOT YET CONNECTED" despite the working Calendar MCP. This arc **replaces that dormant per-user-`'calendar'` check with the per-user-MCP path**. The MCP read primitives (`getOrgMcpExecutionTargets`, `executeMcpTool`) are reusable outside the chat loop, but are org-scoped today — the per-user MCP connection dimension is the arc's core work, not the card wiring.
  - **Also in this arc's scope — the per-user Settings → Connections page (and the landing calendar card) are blind to org-level MCP connections.** Google Calendar and Gmail render "Available soon" on `/workspace/settings/connections` even though they are connected via MCP, because that page reflects ONLY the per-category `connection_grants` model, not the MCP model (`getOrgMcpExecutionTargets`) — the SAME two-connection-systems root cause as the landing calendar card. Reconcile the Connections page, the landing calendar card, and per-user calendar TOGETHER so there is one coherent "what is connected" answer. **This gap now surfaces in multiple places, which suggests the arc may be better framed broadly as a "CONNECTION-SYSTEM UNIFICATION" arc** — one model spanning both the `connection_grants`/category system and the MCP system — of which per-user calendar is one outcome rather than the whole point.

- **ARC — Matters / matter-management connection.** The "Matters" section on the workspace home (`components/workspace/home/matters-section.tsx` + the rich `matters-connected.tsx`) is built-but-dormant behind `isMattersConnected` (Reading-1, D-057/D-058). Unlike Calendar, **there is NO backend at all** — no CLM / matter-management provider, no MCP server, only the dormant view and stub fetchers (`lib/workspace/home/matters-connection.ts` returns `[]`/`null`). Wiring it therefore requires a **real integration** (a provider adapter or an MCP server for the customer's CLM / matter-management tool), so it is a **full integration arc, not a polish item**. (Distinct from the internal matter-tracking surface in item 12, which is legalOS-native tracking rather than a connection to an external system of record.)

- **ARC — Desk. Personal feeds SHIPPED (2026-06-23, D-173); admin-curated role-scoped content is the next additive layer.** The "Desk" section (`components/workspace/home/reading-section.tsx`) is no longer a static empty-state: users add Substack, podcast, and news sources by URL and the Desk renders one card per source with its latest item, server-side cached (45-min TTL) and SSRF-guarded, owner-scoped per user (migration 0075, `desk_feeds`). What shipped is the **user self-select** half. **What remains — the additive admin-curated layer:** admin-curated, role-scoped Desk content that appears alongside a user's own feeds. It lands as a SIBLING source the Desk loader merges into the same `DeskCard` view model and card (the `sourceType` discriminator is already in place), NOT a reshape of personal feeds. **Open design questions to resolve when that layer starts:**
  - **Control model** — the shipped layer is pure user self-select. The admin layer likely lands on the **admin-sets-the-ceiling** pattern used elsewhere (admin defines an allowed/curated set, surfaced by role; the user keeps their own feeds too). Whether admins also constrain which personal sources a user may add is the open question.
  - **Content-sourcing mechanism** — personal feeds use direct RSS/Atom ingestion (Substack works; podcasts work via per-show RSS). The admin layer reuses the same parser/fetch path; Apple Podcasts' directory has no open RSS (per-show feed URLs only), unchanged from before.
  - **Schema** — a sibling table (org-scoped, role/department-targeted, admin-written, its own RLS), merged by the existing loader. The personal `desk_feeds` table is unchanged by it.
  - **UI** — the card grid already replaces the empty state; the admin layer adds curated cards into the same grid (possibly a subtle provenance affordance distinguishing curated from personal).

**Tech foundation — test runner (deliberate, scheduled around 2P-6).** The repo currently has NO test runner (no vitest/jest, zero `*.test.ts`). The Phase 2 pure functions are prime candidates for real assertions: 2P-2's namespacing/truncation/`input_schema` normalization (`lib/connections/mcp/tool-mapping.ts`), 2P-3's result/error shaping (`lib/connections/mcp/execute-tool.ts`), and the trust-derivation logic (`deriveMcpTrustTier`). The deliberate plan is to add a lightweight vitest setup and backfill these tests **around 2P-6**, so the one hot-path change (the gated loop) lands guarded by tests — not now, to avoid bolting test infra on mid-arc. Recorded as an intentional foundation task, not drift (D-101).

Then the OAuth data-source connectors (Mail, Calendar) round out the go-live proof-out, and Phase 2 (the agent tool-use loop, Docs/Sheets export) follows as a separate arc. Models-as-a-connection also leads into the Business model & pricing arc (above), which unblocks A4b.

Go-live proof-out (after flags 1-4): connect real services on the operator's own instance — BYO Anthropic (exercising the call-time credential-resolution path end to end), and Google Drive / Mail / Calendar via MCP if Google offers official first-party servers, else via the existing OAuth path.

### Connections phase — decided architecture (flags 1 & 2)

These two foundational decisions were settled in design discussion. They are durable; the build follows them.

**Models-as-a-connection (flag 1) — decided architecture:**

- A model provider is a new NON-OAuth connection kind, built within a GENERALIZED connection abstraction (the existing OAuth-data-source connector becomes one kind among several). The phase's foundational move is generalizing the connection abstraction to host multiple connection kinds.
- Two ORTHOGONAL axes, kept independent: (1) which provider (an interchangeable provider ADAPTER, Anthropic first; Google, OpenAI, self-hosted as future adapters), and (2) whose credentials (a CREDENTIAL SOURCE: "managed" = legalOS platform key, customer never sees it; "BYO" = customer-supplied key stored in the existing connection_secrets AES-256-GCM substrate). The chat route resolves the credential at CALL TIME based on the org's mode, replacing the always-on ANTHROPIC_API_KEY read. Orthogonality means every combination (managed-Anthropic, BYO-Anthropic, BYO-OpenAI, BYO-self-hosted) works without special-casing.
- Mechanism is KEY-BASED, not OAuth (OAuth is for data sources; model providers authenticate by API key) and not MCP (MCP is a separate kind for tools, not the model mechanism).
- A CONFIGURABLE ENDPOINT / base-URL dimension is baked in from the start (Anthropic's is fixed; self-hosted's is customer-supplied), so self-hosted (open-weight models the customer runs on their own infrastructure, behind a typically OpenAI-compatible endpoint) slots in later with no retrofit. Self-hosted is BYO-only by definition.
- The models.ts canonical source (A2b) is the clean seam: its source swaps from a hardcoded array to "models the connected provider's adapter reports," while all ~8 consumers keep deriving from it. Surrounding work (the real ~80%): per-org call-time key resolution; the abstraction generalization; PRICING HAS NO DISCOVERY SOURCE (a discovered model has no price, pricing must be entered/configured); and the per-model notify-and-approve governance dimension.
- Provider lineup: Anthropic AVAILABLE now (Opus, Sonnet, Haiku, top model per tier); COMING SOON in order: Google, OpenAI, self-hosted.
- UX principle: the "best agnostic model connector", two-axes-legible (which provider / whose key as two clear choices), provider-uniform (every provider the same card/flow/model-reveal), models flow in after the connection is configured. Build against the frontend-design skill.
- Proof-out: BYO-Anthropic on the operator's own instance, to exercise the harder call-time credential-resolution path end to end.

**MCP (flag 2) — decided architecture and trust posture:**

- MCP is a FIRST-CLASS connection kind, built into the generalized abstraction from the start (capability built in, per requirement and the industry's clear consolidation on MCP as the open standard for AI tool integration, Linux Foundation / AAIF governed, backed by Anthropic/OpenAI/Google).
- MCP is PREFERRED for TRUSTED tool/data sources (the maintenance/standardization win: update one server, all agents inherit; capability discovery at connect time), with BESPOKE adapters as the fallback for sources lacking a good trusted MCP server. Performance is neutral (MCP adds a thin hop; it is not faster, chosen for maintenance/standardization, not speed).
- STRICT TRUST BOUNDARY (the security correction, non-negotiable for privileged legal data): "trusted" means ONLY (a) first-party official servers on a legalOS-curated, vetted allowlist, OR (b) customer-self-hosted endpoints the customer configures. ARBITRARY THIRD-PARTY / COMMUNITY MCP SERVERS NEVER CONNECT, the system permits no code path to connect an untrusted server. (Rationale: MCP's security maturity lags its adoption, many community servers carry CVEs, path-traversal, and tool-poisoning risk where malicious tool metadata can mislead an agent; unacceptable for privileged data.)
- ALL MCP connections are GOVERNED by notify-and-approve (the same discipline as models): the trusted allowlist is curated by legalOS and opt-in per org (the super admin approves which trusted servers their org enables); self-hosted endpoints are admin-configured/approved. Nothing connects without explicit admin vetting.
- Pairs with self-hosted MODELS for maximal data sovereignty: a firm can run its own model AND its own MCP servers so privileged data never leaves its environment.

**C4L (Claude for Legal) — decided model (flag 3):**

- C4L is a CONTENT LIBRARY, not a connection (it flows inward from the vendor; "connection" language is reserved for things the customer connects outward to). It is vendor-owned content legalOS ships.
- Default-ON as a value-add; the customer super admin has an OFF switch (so the C4L section stops appearing on their department pages). The customer's control is binary (library on/off), NOT version-by-version.
- NOT real-time/live-synced. Updates are deliberate events.
- Governance authority = legalOS (the owner of the content), NOT the customer super admin, because governance authority matches OWNERSHIP. The customer owns their data/models/tools (so they govern those connections); legalOS owns C4L content (so legalOS decides what ships). Forcing customer-side version approval onto vendor-owned content would be a category error. This avoids customer version fragmentation (a support/quality problem for vendor content).
- The notify-and-approve discipline still applies, but the APPROVER is the legalOS owner (internal), not the customer, a different authority matched to ownership.
- BUILD STAGING: lock the model now; stage the build. Near-term (single-tenant), C4L stays a deliberate operator-managed content set (cleaned up from the manual import script, default-on with the off-switch). The full platform-owner approval + cross-org propagation to all customers is DEFERRED to the platform-owner tier (below), since cross-org propagation is theoretical while single-tenant.
- C4L-content QUALITY monitoring (is the content legalOS ships performing well across customers?) is a PLATFORM-level concern (legalOS's content, legalOS's quality bar), distinct from customer Evals (tenant-level: customers evaluate THEIR OWN agents). Do not merge the two.

C4L placement (decided):

- The super-admin C4L on/off control lives in ADMIN -> Policy & access (it's an org-wide, super-admin-controlled governance decision, consistent with the default model and connection policy living there, NOT a personal Settings item, and NOT in Connections since C4L is content, not a connection).
- It is its own new "CONTENT" section on the Policy & access page, placed AFTER Default model (Allowed connections and Default model are connection/model-layer siblings; Content is a distinct category and sits as its own block after them). Order on the page: Allowed connections -> Default model -> Content.
- The Content section is designed as a LIST OF CONTENT PROVIDERS (not a hardcoded single toggle), even though Claude for Legal is the only one today, because more content providers are likely. Structure: a "Content" subheading + short editorial line, then a list of provider rows (filled-row register), each with name + brief description + on/off control. Super-admin-interactive (optimistic-save idiom), read-only for other admins. Mirrors how allowed_categories is already a toggleable list, so it's consistent and future-proof (a new content provider = a new row, not a redesign).
- USER visibility: regular users need no setting, they simply SEE the enabled content library's agents in their department lists (presence = visibility). No user-facing C4L setting.
- Storage (build-time note, not now): the per-org content-provider enablement state needs an org-scoped store designed for MULTIPLE providers from the start (e.g. a content_provider_settings table keyed by org + provider, or org JSONB), like default_model is org-scoped. Settle at build time.
- A dedicated top-level "Content" admin area (separate from Policy & access) is DEFERRED until there are enough content features to justify it; for now Content is a section within Policy & access.

**ARC — Workflows (the orchestration engine) — COMPLETE (2026-06-06).**

- **Step 1 SHIPPED (2026-06-05, D-115): the headless `runAgent` primitive** (`lib/agents/run-agent.ts`) — a server-side, non-streaming way to run an agent on a task input and get back its output, tool trace, and usage, reusing chat's governed credential/system-prompt/MCP-read-tool resolution under the same gating, never performing an unattended write, and recording `usage_events`. A workflow agent-step calls it.
- **Step 2 SHIPPED (2026-06-05, D-116): the workflow data model + execution engine.** A workflow is a declarative step-graph definition (`lib/workflows/types.ts`: the typed step union agent / tool_action / human_checkpoint, each with a stable id; input mapping defaulting to the previous step's output; router/branching representable as an additive edges overlay, no migration). A pure, dependency-injected engine (`lib/workflows/engine.ts` `runWorkflow`, unit-tested with fakes) walks steps linearly, resolves each input, runs agent-steps via `runAgent` (reads only) and tool_action steps via `executeMcpTool` (READ-classified governed tools only — no unattended writes), pauses at human-checkpoint steps (`awaiting_approval`, no auto-approve), fail-stops, and never throws, emitting one immutable step record per step. The I/O wrapper (`lib/workflows/run.ts`, via the `startWorkflowRun` server action) snapshots the definition per run (immutable to later edits), wires real resolvers from the existing agent + MCP registries (turnkey: a newly-connected tool is an available step with no engine change), and persists `workflow_runs` + `workflow_step_runs` (the audit trail). A data-boundary validator (`lib/workflows/validate.ts`) gates definitions — the same gate a future agent-emitted definition passes. Migration `0060_workflows.sql` (three tables + RLS + `usage_events.workflow_run_id` for workflow cost attribution); **the operator applies it.**
- **Step 3 SHIPPED (2026-06-05, D-117): human checkpoints, approved writes, and the autonomy foundation.** Generalizes the proven Phase 2 chat write-confirmation pattern to workflow runs (`lib/workflows/engine.ts` `runWorkflowSegment` + `resumeWorkflow`; `lib/workflows/run.ts`; the `decideWorkflowApproval` action). A run carries a `autonomy_level` (supervised default — checkpoints + writes pause for approval; autonomous — auto-clears checkpoint gates but writes STILL pause). The engine pauses durably at a checkpoint or before any write, persisting a `workflow_pending_approvals` row (token_ref only for a write, never a token); a decision resumes via an ATOMIC pending→resolving claim (approved write executes at most once via `executeMcpTool` with a live re-resolved token), and `workflow_step_runs.approval_mode` records human-approved vs auto-proceeded. The pure engine has NO write-execution path — a write only runs through the approval-resume path's injected executor, making "no unattended write" structural. The validator now ALLOWS write tool_actions (approval-gated). Migration `0061_workflow_approvals_and_autonomy.sql`; **the operator applies it.**
- **Step 4a SHIPPED (2026-06-05, D-118): the no-code workflow builder + My Workflows surface.** The dormant My Workflows page is now real (`app/workspace/workflows/my-workflows/`): list, create, and edit workflows. A form/list builder (`components/workflows/workflow-builder.tsx`) edits the CANONICAL `definition` jsonb directly (no separate UI format) — an ordered list of steps (run an agent / take an action / human approval), each with a stable id and an input mapping defaulting to the previous step's output. Save goes through `saveWorkflowDefinition` which runs the SAME `validateWorkflowDefinition` the engine uses before persisting (inline errors; empty drafts allowed, active needs ≥1 valid step). Available steps come from live registries (`lib/workflows/capabilities.ts` `getWorkflowCapabilities`: org native agents + governed MCP tools with discovered schemas + read/write classification reusing the chat tool-naming), so a newly connected tool appears automatically; write tools are marked "requires approval". Autonomy is NOT a builder field (run-level only). Org-admin gated. A visual canvas stays additive over the same graph for later. No migration.
- **Step 4b SHIPPED (2026-06-06, D-119): run, watch, audit, and approve — the workflow experience is now end-to-end usable.** Built entirely over the existing engine actions (no engine change). A run-start page (`app/workspace/workflows/my-workflows/[id]/run`, plus a "Run" affordance on active My Workflows cards) lets any member who can read the definition provide the run input and choose the RUN-level autonomy (supervised default; even autonomous runs pause before any write), with the workflow's recent runs listed below. The run view (`app/workspace/workflows/runs/[runId]`) is the observability + legal audit surface: run header (status / autonomy / who / timing) and the step-by-step timeline — the frozen `definition_snapshot` merged with the immutable `workflow_step_runs` rows (pure, tested `lib/workflows/run-view.ts`) — showing each step's friendly type, status, input/output (long content behind a disclosure), timing, and approval provenance (human-approved vs auto-proceeded, personalized to the viewer). A paused run surfaces an approval card mirroring the chat write-confirmation (checkpoint prompt, or the write's friendly tool label + argument key NAMES only — never values); Approve resumes (the write executes through the governed executor), Deny cancels; owner-only decisions (RLS), with a lost atomic claim settling calmly as already-decided. Refresh model: persisted state + a quiet visibility-gated poll while non-terminal (no streaming). No migration.
- **Workflow Builder Tier-1 polish SHIPPED (2026-06-06).** Live-QA fixes to the Step 4a builder — presentation/UX only, over the unchanged canonical definition + engine: the department/agent/status/input-source selects now resolve their trigger labels via Base UI's `items` prop (the trigger previously rendered the raw stored value, i.e. UUIDs); the tool picker is a searchable, server-grouped combobox (`components/workflows/tool-picker.tsx`, Base UI Combobox styled to the app Select) labeled with the chat tool-display names plus a clean "Requires approval" tag, full-width so labels never truncate; builder fields use the app's `bg-paper-2` input fill (the established admin-form treatment) so they read as editable; the input-source picker explains each choice in plain language, with "Custom value" revealing a field for a fixed value (the ValueSource model unchanged); and a workflow can be deleted from the builder behind a confirmation — a HARD delete, the 0060-designed path (runs FK is set-null + every run keeps its own `definition_snapshot`), so run history survives intact and viewable (D-120). KNOWN ITEM for the post-arc functionality/delight pass: tool-action step INPUTS are still too technical for a no-code user (per-argument "Not set" source configuration for args like cc/bcc/htmlBody) — redesign the tool-input UX for a non-technical user.
- **Delight pass D1 SHIPPED (2026-06-06, D-121): the pausable headless agent loop.** The keystone of the agent-centric builder direction ("C weighted toward A"): `runAgentLoop` can now PAUSE on a proposed write instead of refusing it, under an explicit `writes: "pause"` policy (default `"refuse"` keeps every existing caller and chat byte-identical). A pause returns the full proposed action (the chat-shared `PendingMcpToolCall` shape — full args kept for the later show-content disclosure, argKeys for the default PII-safe render) plus resumable loop state; `resumeAgentLoop` executes an approved write ONCE via the injected governed executor and continues (multi-pause supported), or on deny feeds the shared graceful "declined, do not retry" result so the agent acknowledges and finishes (Fork 1: the step completes). The loop has no write-execution path of its own; the at-most-once guarantee stays in the host's atomic claim. Loop-level only, fake-tested in isolation; no engine wiring, no UI, no migration.
- **Delight pass D2 SHIPPED (2026-06-06, D-122): agent steps act under human approval in the engine; the audit-trace gap is closed.** An agent step now runs the pausable loop (`writes: "pause"`): a proposed write pauses the RUN — the agent's resumable loop state + the FULL proposed action persist to `workflow_pending_approvals` under the honest new kind `'agent_write'` (token_ref pointers only), the step + run go awaiting_approval, and the 4b approval card renders it PII-safely (keys only; show-content disclosure is D3). The decision resumes the agent's own loop via `resumeAgent`/`resumeAgentStep`: approve executes the write once through the governed executor and the agent continues (multi-pause supported — the step stays awaiting across repeated writes); deny lets the agent finish gracefully and the step COMPLETES, the RUN CONTINUES (Fork 1 — distinct from a tool_action write deny, which still cancels). Every step now persists its PII-safe tool-call trace to `workflow_step_runs.tool_calls` (reads and writes, with approved/denied provenance), closing the audit gap; pre-migration step writes degrade gracefully (retry without the trace). The engine still has NO write path of its own; supervised AND autonomous runs both pause for an agent-proposed write. **Operator: apply migration `0062_agent_write_approvals_and_step_traces.sql`** (the `agent_write` kind + the trace column). No validator/authoring change — agents propose writes at runtime; approval gates them.
- **Delight pass D3 SHIPPED (2026-06-06, D-123): the agent-centric builder + the show-what-it-will-send approval. THE DELIGHT PASS (D1–D3) IS COMPLETE.** An agent step now takes an OPTIONAL plain-language instruction ("Review this NDA and flag unusual terms") that layers on the agent's stored system prompt — composed with the mapped input at run time by the pure, tested `composeAgentTask` (`lib/workflows/agent-task.ts`: directive, then the input in a delimited `<step_input>` block; no instruction → byte-identical to pre-D3; rides the definition jsonb, NO migration). The builder leads with the agent (primary "+ Run an agent", de-emphasized "+ Take an action (advanced)", an agent-centric empty state), the tool_action form is the legible advanced path (per-arg descriptions now rendered; required args up front, optionals behind a "More options" disclosure — pure, tested `splitToolArgs`), and composition is guided: a live plain-language readback panel ("What this workflow does", pure tested `workflowReadback` in the new `lib/workflows/builder-view.ts`), a run-input explainer, a draft/active explainer, and "Save and run" (active workflows) straight to the run-start page. The approval card, for AGENT-proposed writes only (D2 persists the full args), gained an optional "Show what it will send" disclosure revealing the actual argument values — keys-only default; explicit tool_action writes stay keys-only. UI + the additive instruction primitive only; engine/approval mechanics/autonomy/chat unchanged. No migration.
- **Step 5 SHIPPED (2026-06-06, D-124): the Template Library + agent-centric starter templates. THE WORKFLOWS ARC IS COMPLETE.** Templates are org-specific, forkable workflow_definitions flagged `status='template'` (migration 0063: the widened status check + a `template_slug` seed key) — same shape, same validator, structurally NOT runnable (running requires 'active': fork first). The Template Library route (`app/workspace/workflows/template-library`) replaces the landing's coming-soon card: each template reads at a glance (name, description, the D3 plain-language "what it does" readback), and "Use this template" forks it via the existing validated authoring path into a user-owned DRAFT that opens in the builder (templates are excluded from My Workflows; the edit page refuses to open one). Three starters seed via `npm run seed-workflow-templates` (operator-run, service role, idempotent on (org, template_slug); pure tested specs in `lib/workflows/templates.ts` reference agents by stable SLUG, resolved live — a template whose agent is missing is skipped with a report): "Review an inbound NDA" (C4L nda-review + checkpoint), "Review a contract (any type)" (the FLAGSHIP: ONE instructed agent step — enterprise-agreement-review — classify-and-review inside the agent's reasoning, the agent-handles-routing replacement for the filtered C4L `review` router, NO branching engine), and "Review and respond" (review → checkpoint → C4L stakeholder-summary drafts the counterparty email and, with approval, sends — exercising D2's gate + D3's show-what-it-will-send end to end). Org-specific Option-A templates now; the slug-based spec + per-org resolution is the additive seam for portable cross-org recipes later. **Operator: apply `0063_workflow_templates.sql`, then `npm run seed-workflow-templates`.**
- **UX consolidation (2026-06-06, D-125): My Workflows and the Template Library merged into ONE adaptive Workflows screen** (the arc remains complete; this is composition, not capability). The user's workflows lead, templates sit below in a collapsible "Start from a template" section (persisted per user), and with zero workflows the templates ARE the empty state — the first-run path leads straight into a starting point. The rail's Workflows group folds to a single "My Workflows" leaf; the old /workspace/workflows/template-library route permanently redirects to the combined screen. Fork-only template affordance unchanged.
- **Deliberately deferred (recorded at arc close):** branching/router execution (the agent-centric instruction handles review-routing; branching returns only with a real case the instruction can't carry); portable cross-org template recipes (multi-tenant era); scheduled/webhook triggers (the matter-management ideal-state lever); the gated full-autonomy write tier (autonomous runs still pause for writes by design); Help → Guides content (bare scaffold needing its own design pass); structured field-extraction / JSON-path arg mapping (avoided via the agent-centric path; revisit only if a real need survives it); ai-tool-handoff / external-connector templates (Luminance/Kira not connected — study/adapt when they are).

During C4L import, several C4L skills that are NOT "agent-shaped" — routers, config/cold-start skills, orchestration/workflow-oriented skills — were filtered out of the department-agent UI (soft-deleted: `is_active=false`, `deleted_at` set) via the `0024`-style filter migrations, and tracked (with their intended destination surfaces) in `docs/C4L_DEFERRED_SKILLS.md`. These "orphaned"/deferred skills carried the workflow implications that motivated this arc. **LARGELY REALIZED by the completed Workflows arc (2026-06-06):** the Workflows section is fully built (engine, builder, run/audit/approve, agent-centric delight pass, Template Library), and the flagship router case — the `commercial-legal/review` router composing the leaf review agents — shipped as the "Review a contract (any type)" template, realized agent-centrically (one instructed agent classifies-and-reviews in its own reasoning) rather than as branching execution. **What remains of the exploration:** the rest of the deferred-skills catalog (`docs/C4L_DEFERRED_SKILLS.md`) has not been swept template-by-template — config/cold-start skills (a possible org-playbook seam), the remaining orchestration skills, and any that genuinely need branching or external connectors (ai-tool-handoff) await their surfaces; mine the catalog for further templates as real usage shows which are wanted.

**Discovery automation (flag 4) — decided principles (build deferred, riding the platform-owner tier):**

- Universal shape: detect an upstream change -> the authority that OWNS the thing reviews it -> explicit choice to apply -> audited. NEVER auto-apply (notify-and-approve, generalized).
- DETECTION IS MANUAL-TRIGGER near-term (the key refinement): the platform owner presses a "check for updates" button that runs the discovery queries ON DEMAND (against the provider Models API; against the C4L GitHub repo), reports what has changed, and offers the choice to act. This deliberately AVOIDS the net-new scheduled-task infrastructure (no cron, webhooks, or background jobs in v1), a button running a query on click is a fraction of the work and proves the valuable part (the detection logic) first. Scheduled/webhook automation is a DEFERRED later enhancement ("if you tire of pressing the button"), not v1.
- Two-stage, authority-matches-ownership:
  - PLATFORM OWNER discovers and decides what to OFFER (add a newly-released model to the platform model/connector CATALOG; ship a new C4L content version) via the manual-check button. This is the platform-tier action.
  - CUSTOMER super admin separately decides what to ENABLE for their org (a newly-available catalog model -> the tenant-side notify-and-approve in Policy & access). Two authorities, two stages: platform governs what's OFFERED, customer governs what's ENABLED.
- The core deliverable is the DETECTION LOGIC (the queries that correctly determine "what's new" from the Models API and the C4L repo); the trigger is just a button for now. A general NOTIFICATION PRIMITIVE (the system surfacing a pending item to an authority, which does not exist today; the audit tables record events but nothing notifies) is a recognized foundational prerequisite for any future PUSH/scheduled notify-and-approve, built when first needed and designed generally.
- Detection-value asymmetry (rationale): automation's value is surfacing what you would NOT otherwise know. For C4L (the owner authors the repo) the owner already knows changes happened, so manual-check suffices indefinitely; for MODEL releases (a provider authors them) detection genuinely adds value (you don't know when Anthropic ships a model), so model discovery is the stronger candidate for eventual scheduled automation.
- BUILD STAGING: flag #4 rides its dependencies, model discovery builds with/after models-as-a-connection; C4L discovery builds with the platform-owner tier (where the owner's manual-check button and the catalog live). Not a standalone near-term build; lock the principles now.

This completes the Connections-phase flag decisions (flags 1-4 all decided).

Tracked so they are not lost; each builds on the connector infrastructure shipped in the connector hub arc.

- Agent-form Drive picker: the Drive picker shipped in the chat composer (message attachments) only. Adding it to the agent form (agent attachments) reuses the same picker component. Dependency to clear first: the agent EDIT page must render `gdrive_link` agent attachments gracefully in its attachment display. Today's rendering predates Drive attachments, and a `gdrive_link` agent attachment is only creatable by hand-insert; once the agent-form picker exists, real ones will appear and the edit page must display them without error.
- Admin connection-policy editing UI: the super-admin surface to edit the connection policy, built in the Admin polish arc (item 1) where the admin IA is designed. Until then the `connection_policy` row is edited directly in the database.
- Calendar and Gmail connectors: each is a new provider adapter reusing the existing OAuth flow, the single provider-agnostic callback, the token-exercise layer, and (for a picker) the listing layer. The home's Today (calendar) and Matters connected-state views are dormant behind `isCalendarConnected` / `isMattersConnected`, ready to light up when those connectors ship. The Calendar, Gmail, and Slack Connect CTAs on the Connections page are inert today (no adapter yet). A compact connection-status strip on the home (D-061) is a possible later addition once multiple connectors exist.
- Drive picker refinements: pagination and scoped-in-folder search (the picker uses single capped pages plus global search today); additive enhancements if needed.
- Models as a connection (Anthropic first): available models derive from connected model providers, and the org default becomes selectable from any connected model. The canonical models source (lib/llm/models.ts, shipped in A2b) is the single seam to swap when this lands; validation and every picker follow without change.
- Model discovery automation: derive the available-models list from the provider's Models API rather than the hand-maintained canonical list, so new models surface automatically instead of via a code edit.
- Notify-and-approve model/capability lifecycle (LOCKED decision): newly discovered models, and new C4L capabilities, NEVER auto-enable. The super admin is notified and the model/capability activates only on their approval. This is the locked governance posture for anything that would otherwise silently change what the org can use.
- C4L reframed as a connection: Claude for Legal becomes a connection (its own capability category, or a provider, TBD) rather than a one-off import path, so its capabilities flow through the same connect/approve governance as other connections.
- Connection mechanism: API vs CLI vs MCP, to decide in the Connections phase (which transport a connected model provider uses).
- Model-retirement handling (prerequisite before curating the model list down): no model may be RETIRED until graceful handling exists, either a graceful fallback, or flag-and-require-choice (surface affected agents and require the admin to pick a replacement) before the retirement takes effect. The operator leans toward the flag-and-choose options (2/3). Models can be ADDED today, but the canonical list cannot be curated down until this lands. A2b deliberately added only Opus 4.8 and left Sonnet 4.6 / Haiku 4.5 untouched; checking for newer Sonnet/Haiku versions is a one-place edit when wanted.

The remaining Share destinations from the original connector item stay tracked here: per-message Export to (Google Docs, Excel, PDF) and Send to (Slack, Gmail, Outlook, Apple Mail, Messages / SMS, Box) via the kebab destination hub (D-054), and full-conversation export as a conversation-level kebab item (deferred per D-054 until the hub ships its second destination). Most destinations depend on the connector infrastructure now shipped; PDF (rendered locally) and mailto: links can ship without a connection.

## 3. Skill library surface (C4L pattern #5)

C4L plugins ship with reference SKILL.md files (Pattern #5 in docs/C4L_DEFERRED_SKILLS.md) that aren't exposed in product UI today. A skill library surface would let users browse and use the patterns and reference materials from C4L plugins. Related to item 11 (Template Library); both are reusable-artifact libraries and could share design thought or even a UI shell.

## 4. Out-of-scope C4L plugins (per D-051)

Four C4L plugins (law-student, legal-clinic, legal-builder-hub, cocounsel-legal) remain deferred per D-051's trigger conditions: academic/clinical user demand OR strategic-priority signal. These don't fit the current department taxonomy and would require new infrastructure (a new rail group, a new entity type).

**Updated 2026-06-10 (connector catalog arc, D-150):** the plugins' CONNECTORS are no longer deferred with them — the connector catalog harvested every connector their `.mcp.json` configs ship (CourtListener, Descrybe, Courtroom5 from the clinic/student plugins; Lawve AI from legal-builder-hub), and cocounsel-legal's Thomson Reuters connector lands in the catalog when a customer brings a TR subscription. The plugins' AGENT CONTENT remains deferred: law-student and legal-clinic moved to item 10a as a deliberate later consideration (per the operator); legal-builder-hub and cocounsel-legal stay here under the D-051 triggers.

## 5. Privacy and security

Broad initiative covering multiple sub-areas. Specific scope to be determined when work starts. Likely candidates: RLS audit (ensure all tables have correct policies and that policies match intent), security review of auth flows, data retention policies, user privacy controls (export/delete account, consent management), GDPR/CCPA readiness if targeting those jurisdictions, SOC 2 prep if planning to sell to enterprise. Some of these are quick audits; some are major undertakings.

**Research-run retention (noted 2026-06-11, D-154):** manual deletion of research runs exists (the asker and admins; findings cascade, the usage ledger survives), but an automatic RETENTION POLICY (expiry of runs/findings) was deliberately deferred to this arc, where deletion-and-return obligations are designed as a whole rather than per-feature.

## 5a. Data capture for measurement (new arc, prerequisite for richer analytics)

Surfaced by the analytics-arc design-check. The product measures ACTIVITY richly (native-agent runs, real cost, tokens, model/tool mix, workflow completion/timing/autonomy/approval, adoption denominators) but captures NO outcome or value signal, and has blind spots. The gaps an operator would want but that are not captured today: no "task completed" / "time saved" / outcome signal anywhere (the only "completed" is a workflow graph finishing, not useful work done); no per-matter or per-document outcome; no session/login/last-active signal, so there is no true DAU/WAU (active users can only be inferred from `usage_events`, i.e. native-agent runners only); external-agent (Gemini/watsonX/custom-link) clicks write no server-side row, a blind spot for the large external tier (overlaps item 17's localStorage→Supabase promotion); no general per-event analytics ledger; and no quality/satisfaction signal (no thumbs-up/down; Evals A5 deferred). This arc decides which of these to start capturing, and how, before the analytics that depend on them. Until then the analytics surfaces measure what genuinely exists (activity), and ROI stays a manual-input estimate.

## 5b. European market and AI sovereignty (strategic option, gated on the Europe go-to-market decision)

Recorded as a strategic option, NOT scheduled work. This becomes active only if/when Europe is a near-term target market; until then it lives here so the thinking is not lost to conversation memory.

**The opportunity.** European legal departments and firms increasingly treat digital/AI sovereignty as a GATING requirement, not a nice-to-have: data residency (where data physically lives and is processed), GDPR and EU AI Act posture, independence from US-controlled infrastructure (CLOUD Act exposure being the recurring concern), and the ability to run models on infrastructure the organization controls (self-hosted, EU-cloud, or open-weight). For this buyer, "where does it run and who can compel access" can decide the deal before features do.

**The honest current gap (so future work is scoped against reality, not the Mission page's aspiration).** The Mission page frames "sovereign legal AI" as the DIRECTION we are walking toward, which is honest and stays honest. But the hard sovereignty claims are NOT shipped today: the models that actually run are Anthropic's (US-based), there is no on-prem or EU-resident legalOS deployment, and the open-weight/local-model path is architectural scaffolding, not a shipped option. What IS real is the foundation: the product is model-agnostic in architecture (`lib/llm/models.ts` as the seam), supports bring-your-own model key under the customer's own provider agreement (D-087), and connects only first-party or customer-self-hosted MCP servers (D-089). That is the foundation, not the finished story, and this item must be scoped against that reality.

**This item contains two work streams.**

1. **Messaging.** Sharpen how Mission, the Trust Center, and possibly the Features control section speak to the European sovereignty concern explicitly and in EU buyers' vocabulary (data residency, regulatory posture, infrastructure control), always as honest direction-plus-architecture rather than a shipped guarantee. This is the cheaper stream and can move first, but on its own it does not make sovereignty true; it only states the direction more precisely for this audience. Bound by the standing rule that marketing claims never drift ahead of the architecture (D-126).

2. **Product and compliance.** The real work to make sovereignty TRUE for an EU buyer: EU data residency, a documented GDPR / EU AI Act compliance posture (a Trust-page concern, related to item 5), support for EU-hosted or open-weight models (the models-as-a-connection arc in section 2 is the natural seam), and a self-hosted / EU-cloud deployment story. The credible version of sovereignty is partly an engineering and compliance arc, not a copy change; the messaging stream must not outrun it.

**Relationships:** sits beside item 5 (privacy and security: GDPR/CCPA readiness, retention/deletion, SOC 2) and the data-sovereignty story consolidated in `docs/SECURITY_ARCHITECTURE.md`; the product stream draws on the Connections phase (models-as-a-connection, section 2) and the self-hosted-MCP + self-hosted-model pairing already noted there as the maximal-sovereignty posture.

## 6. Full code review / bug sweep / maintenance polish — COMPLETE except deferred-future (2026-06-08)

Comprehensive product audit from a maintainer-delight standpoint. Likely candidates: TypeScript strictness audit (any/unknown usage), dead code removal, unused dependency cleanup, test coverage analysis (write missing tests for critical paths), linting consistency, accessibility audit (WCAG compliance), performance audit (lighthouse scores, bundle size, query performance), error handling review (especially around the chat route and Supabase queries). Could be broken into focused passes, one commit per area.

**Done so far.** A read-only health census mapped the codebase and docs. The two must-fix correctness items it surfaced shipped as the per-organization connection scoping fix (D-136, the security commit above). The **documentation truth pass (cleanup Tier 2) SHIPPED (2026-06-08, D-137):** CLAUDE.md, README, PROJECT_OUTLINE, CHATBOT_HANDOFF, SETUP, DEMO_ACCESS_SCOPING, and SECURITY_ARCHITECTURE were corrected to match the current state, three overstated CLAUDE.md security claims were reframed honestly, D-130 was marked partially superseded, and an obsolete TODO was removed. The **mechanical hygiene pass SHIPPED (2026-06-08, D-138):** the lint signal is meaningful again (the `docs/design/` historical artifacts are excluded from ESLint and the one real `chat-interface.tsx` finding is resolved, dropping the baseline from 17 to 0), perf item 16 is done (the four per-navigation `getUser()` calls collapsed into one request-scoped `getAuthUser()`, with the org-role checks derived from the shared profile and behavior preserved), the divergent `resolveSiteUrl` is unified on the trailing-slash-stripping behavior, and the four genuinely-dead files were removed. The **targeted tests SHIPPED (2026-06-08, D-139):** the app-layer privilege-escalation gate (`lib/actions/admin-roles`) and the workflow run orchestrator (`lib/workflows/run.ts`) are now covered (26 new tests; the suite is at 217). A useful finding: the privilege gate is genuinely enforced at the APP layer (authority, org-scoping, the super_admin-grant restriction, the org_admin-cannot-touch-a-super_admin rule, and the last-active-super-admin lockout), not merely deferred to the database trigger. **The item-6 cleanup pass is now complete** except the explicitly-deferred future investments: a first live-Postgres RLS integration harness (to assert the org-fenced policies and the role trigger at the database layer), and a generated-Supabase-types pass to remove the `as never` casts.

## 7. Build out Knowledge, Workflows, and Integrations

The three rail groups currently route to coming-soon placeholders for most leaves: Knowledge (Research, Vault, Sources), Workflows (My Workflows, Template Library), and Integrations (Connections, Marketplace). Each leaf is its own product surface and at least one is a multi-stage arc in its own right. The full build-out is likely multiple separate arcs, sequenced by which leaves earn priority first.

Template Library overlaps with item 11 (Template Library concept definition), which calls out the same surface; the two items merge when this work starts. Connections under Integrations overlaps with the now-shipped connector hub — the workspace-level OAuth infrastructure it built powers the Connections surface here. Sources under Knowledge may overlap with the citation work from the Word export arc.

Scope conversation needed before any engineering starts. The work is large enough that picking one leaf to ship first (rather than building all seven sub-surfaces in parallel) is the right discipline. **The Knowledge half of this item is COMPLETE (the Knowledge arc below, Steps 1–4 + housekeeping, D-152 through D-156); Workflows shipped as its own completed arc (D-115 through D-125). What remains of this item is the Integrations group's Marketplace leaf and any further leaves the groups grow.**

### Knowledge arc — build steps (COMPLETE, 2026-06-11)

The institutional-question engine over admin-defined, transparently-sourced collections that map onto connected external repositories. Design settled by the 2026-06-11 design-check: legalOS is the intelligence layer over the repositories customers already have (no content migration); collections reference stable folder ids through the org's MCP connections; the v1 index is a document INVENTORY only (no derived content persisted, no scheduler — admin-clicked sync, with research runs re-enumerating live so staleness only affects preview numbers); department visibility is the enforcement layer over org-credential reads. **The arc shipped end to end in one day (Steps 1–4 below, plus the run-housekeeping commit D-154 and the ask-surface polish commits 7576347/28896af/f43420d), closing with the external surfacing per the standing done-definition (D-156).**

- **Step 1 — the Collections foundation (SHIPPED, 2026-06-11, D-152).** Migration 0070 (collections / collection_departments / collection_sources / collection_documents, org-fenced RLS with super-admin write and department visibility enforced in RLS); the `canEnumerate` capability flag on the connector catalog (Drive verified live, Box per documentation; only enumeration-capable connected servers can back a source); the admin Collections surface under Knowledge (create/edit/delete, the live MCP folder browser storing stable ids with cached display provenance recomputed at sync, the segmented inventory sync with honest present/missing marking and bounded per-request budgets); the member read-only view; and the Knowledge restructure (Research + Collections; Sources and Vault retired). **Operator: apply migration `0070_knowledge_collections.sql`**, then create a collection over a real Drive folder, sync it, and confirm inventory, provenance, the non-admin read-only view, and department-visibility hiding.
- **Step 2 — the research engine + the Research surface (SHIPPED, 2026-06-11, D-153).** The deterministic segmented sweep, explicitly NOT the chat agentic loop: live enumeration (reusing the Step-1 sync walk with collecting deps), document reads through the adapters with a research-sized 60k-character budget (not executeMcpTool's chat cap), fixed-contract batch classification, code-built citations in the sources idiom, and one synthesis call; each `advance` invocation is one bounded segment (~12 documents) with idempotent findings upserts (run_id + external_id) and a persisted cursor, driven by the client loop, resumable after interruption, cancellable between segments. Honesty throughout: fetch failures, truncated reads, and unsupported types are findings and basis-line facts, never silent. Guardrails first-class: pre-run cost/time preview (stated assumptions), the per-run document cap on `organizations.research_document_cap` (Policy & access "Research" control; over-cap declines honestly), two concurrent runs per org (stale runs uncounted after 30 minutes). Migration 0071: `research_runs` + `research_run_findings` (owner-write RLS; the asker plus org/super admins read — the work-product-belongs-to-the-org stance), `usage_events.research_run_id` with `agent_id` relaxed nullable (the 0060 precedent; the Cost views and Insights already tolerate it). The Research leaf is live (ask, scope picker with transparent provenance, preview, live progress with partial findings, answer with citations, history with reopenable runs). **Operator: apply migration `0071_research_runs.sql`**, then ask a real scoped question, watch preview → progress → findings → cited answer, check `usage_events` rows carry `research_run_id` and platform Cost shows the spend, try cancel on a second run, and confirm a non-visible collection can't be selected.
- **Run housekeeping — deletion + export (SHIPPED, 2026-06-11, D-154).** Runs are managed artifacts: the asker (and org/super admins) delete settled runs with findings cascading and the usage ledger surviving (research_run_id SET NULL since 0071); settled runs export to Word through the existing formatted_outputs pipeline with the chat kebab gesture, the memo carrying question, scope provenance, the cited answer, the basis line, the review framing, and the findings as the evidence. Migration 0072 (formatted_outputs anchors + the admin delete policy). **Operator: apply `0072_research_export_and_deletion.sql`.**
- **Step 3 — the agent tool (SHIPPED, 2026-06-11, D-155).** `research_collections`, a NATIVE server-executed tool in the existing chat loop (a sibling of the MCP tools, dispatched before the routing-map lookup, never routed to a server). Scope resolves through getVisibleCollections() under the asking user's session — the same RLS path as the surface, never a parallel one. Inline cap 15 documents (no segmenting inline; the engine compressed: enumerate live + read + classify, with the agent itself synthesizing from per-document determinations — no plan or synthesis calls); over-cap returns an honest handoff to the Research surface written to relay well. Citations join the conversation's sources (rendering and Word export carry them); usage folds into the chat turn's summed usage_events row (inline = chat work, no research_runs row); governance is the same loopEngaged gate as MCP tools (flag + mcp category policy + connected server). FOLLOW-ONS named: async research-from-chat via the mcp_paused_runs pattern (v2), and CourtListener as a PUBLIC-CORPUS research scope (a research source, not a collection source — it is not an enumerable customer repository).
- **Step 4 — surfacing (SHIPPED, 2026-06-11, D-156). THE KNOWLEDGE ARC IS COMPLETE.** The /features tour gained a Knowledge section (anchored, after Connections — it is built on them) telling the story honestly: collections over the repositories the team already uses with every source visible, citation-backed answers read live with nothing copied or stored, the same capability in chat; the README reflects the layer with the engineering nuance (inventory-only persistence, findings not content).

**Knowledge follow-ons (named at arc close, each where it lives):**
- **Follow-up refinement** — asking against a prior run's result set, thread-like beneath an answer; the ask composer was built reusable for exactly this. The arc's named NEXT BUILD.
- **The profile index** — opt-in per-collection document profiles persisted for cheap repeat questions (the design-check's v2 acceleration), gated on a real usage signal, with its own privacy disclosure.
- **Async research-from-chat** — the in-chat tool starting a real research run and the conversation resuming on completion (the mcp_paused_runs pattern); v2 of the agent tool (D-155).
- **CourtListener as a public-corpus research scope** — a research SOURCE, not a collection source (it is not an enumerable customer repository); pairs with the operator's pending live vet of the connector (D-150).
- **Retention policy** — automatic expiry of runs/findings, deliberately deferred to item 5 (noted there, D-154).
- **Per-user credential passthrough** — the existing named connection-system arc; until it lands, ACL-sensitive and personal corpora stay out of collection scope (D-152).

## 8. Auto-fork pattern

The C4L hybrid-edit pattern (admin edits to C4L agents preserve original C4L provenance while letting the admin customize) works today, but the pattern is manual and gate-based. An "auto-fork" refinement would streamline or restructure it: possibly automatic fork on first admin edit, possibly different fork semantics, possibly making the hybrid-edit pattern more transparent to admins. Scope to be defined when work starts.

## 9. Recents panel

A left-side panel showing recent conversations with the current agent (agent-scoped, not workspace-scoped, per the design decision during the chat page redesign session). Same shape as Claude.ai's recent-chats sidebar.

The Recent section that previously surfaced cross-agent recent conversations on the workspace home was removed in the workspace home revamp arc; its component (ContinueWorkingSection) is retained unmounted to feed exactly this panel. The Recents panel on the agent page is agent-specific: when a user is inside an agent's chat surface, they should see their own conversation history with that agent at a glance.

Requirements: panel positioning (left of the message thread, persistent across conversation switches), the conversation-list query (filtered by agent_id, ordered by updated_at), click-to-resume behavior, the current conversation highlighted, and possibly a "New chat" button at the top.

## 10. Brand mark concentric circles upgrade

The rail brand mark currently uses a 7px filled dot placeholder + "legalOS Workspace" wordmark. The concentric circles motif from the landing page (the marketing visual identity) would be a delightful brand-continuity moment between marketing and product if scaled and refined for the rail. Slot reserved; visual design needed before engineering. Governed by the brand-scarcity principle (D-053): the rail brand mark is the natural third placement for the motif, after the landing page and the chat ThinkingGlyph.

## 10a. Law-student and legal-clinic plugins (later consideration)

The two academic/clinical C4L plugins, deliberately slotted here (lower priority, per the operator) rather than left implicit in item 4. The D-051 re-open triggers stand: academic/clinical user demand OR a strategic-priority signal. Two observations to carry into that conversation when a trigger fires:

- **Legal-clinic's bones resemble small-firm practice management.** Case intake, deadline calculation, procedural next steps, and self-represented-litigant support are structurally the daily shape of a small firm's practice, so the clinic plugin doubles as a probe of a small-firm product direction, not just an academic niche.
- **Their connectors are already in the catalog.** CourtListener, Descrybe, and Courtroom5 were harvested into the trusted-registry connector catalog (D-150), so reviving these plugins is content + taxonomy work; the tool integrations are pre-seeded.

## 11. Template Library concept definition — CLOSED (Workflows arc Step 5, 2026-06-06)

Resolved as workflow templates: org-specific, forkable starter workflows shipped as the Template Library (D-124), then merged into the adaptive My Workflows screen (D-125). The original concept question (workflow vs agent vs prompt templates) was decided in favor of workflow templates; agent/prompt template libraries remain related to item 3 (skill library surface) if ever pursued.

## 12. Tracker-UI surface

Structured matter-tracking surface: a portfolio of legal matters with status, deadlines, action items, ownership. The litigation-legal C4L plugin's matters/_log.yaml schema (flagged in polish #8 as "the single most valuable design reference across all nine plugin imports") provides a free 50-line schema spec for the matter portfolio data model. Substantial product work; legal teams universally need matter tracking. Use the litigation plugin's schema as design reference rather than reinventing.

## 13. Managed-agent Option B API

Architectural decision and implementation for how managed (vendor-imported) agents are stored, updated, and re-imported when vendors push new versions. C4L plugins are the current concrete instance; the pattern generalizes to any vendor-shipped agent. Option B (specifics in C4L design history) is the chosen direction; implementation deferred. Infrastructure work, not directly user-visible but foundational for scaling C4L beyond the current 9 plugins.

**Also in this arc's scope (recorded 2026-06-10):** the C4L `managed-agent-cookbooks` (scheduled watchers and similar recurring recipes). They are the upstream's managed-agent layer, so they belong to this arc rather than a separate item; run a fresh roadmap analysis of the cookbooks when this item is reached. The connector catalog (D-150) already gives future watchers ready connectors to read from.

## 14. Signup flow that captures display name

The current signup flow doesn't capture a user's full_name; the column stays null in public.users. Discovered when the personalized home hero showed "Welcome back." with no name for the operator's own account. Every new user signing up today has the same problem. Fix: modify signup to collect a name (at signup time or as a required first-login step), store in public.users.full_name. Real production gap for any multi-user deployment.

## 15. Claude Skills analysis

Extract lessons learned from this project for the Claude Skills template repo. Concrete intent: identify patterns or capabilities developed during legalOS that could become reusable Claude Skills for future projects. Requires actual analysis to identify candidates; could be 1 skill or 5. Examples of what might surface: the dual-delight standard as a skill, polish-list discipline as a skill, specific UX patterns (asymmetric motion tokens, three-tier active state, coming-soon vs locked treatment), Supabase + Next.js App Router patterns, or session-handoff patterns. Output: skill candidates with rough scope, ready to be migrated to the Claude Skills template repo.

## 16. Auth layer optimization — SHIPPED (2026-06-08, D-138)

The workspace layout was making four sequential `supabase.auth.getUser()` network calls per render (it had grown to four with the platform-owner check), plus several table reads, all on the critical path of every navigation. Fixed in the item-6 mechanical-hygiene pass (D-138): the calls now share one request-scoped, `cache()`-wrapped `getAuthUser()` (so `getUser()` runs once per request instead of four times on the workspace path), and the admin / super-admin / org-admin checks derive from the already-cached `getCurrentUserProfile()` rather than re-querying. Behavior is identical; this is a real wall-clock reduction on every workspace navigation. The proxy's own `getUser()` stays separate (it runs in the middleware execution context, a different client, and cannot share the page-render cache).

## 17. Analytics promotion from localStorage to Supabase (per D-010)

The current analytics_events writer (lib/analytics/events.ts) is localStorage-only per D-010's Phase 2 deferral. Promotion to Supabase: create an analytics_events table, migrate the writer to make server-side inserts, update admin metrics views to read from the new table. Real benefit: per-user analytics, cross-device, server-side aggregation, admin metrics quality improvement. Prerequisite for richer admin analytics in item 2 (admin section revamp).

## 18. Agent versioning (audit trail)

Tracking who created / edited / deleted / forked which agent and when. No audit_log table or version history table exists today. Scope is unclear and depends on the use case: full audit log (every change with diff), version history (rollback to previous), or just provenance (who created this, when). Each has different complexity. Decide scope when the use case becomes concrete, likely surfacing during item 2 (admin section revamp) when admins ask "who changed this agent?"

## 19. Rename the /workspace URL namespace to /home

Deferred and low priority. The product calls the home "Home" (the breadcrumb's first segment was renamed from "workspace" to "home" via the HOME_SEGMENT constant during the workspace home arc), but the URL namespace is still /workspace. Renaming it to /home would mean a directory rename of app/workspace (about 21 route directories), updating every internal link, redirect, and proxy reference, and adding a legacy redirect from /workspace/* to /home/* so existing links and bookmarks do not break.

This is cosmetic only for a gated app: the URL-label mismatch is not an inconsistency, because URLs are infrastructure and labels are product language, and the two are allowed to differ. Captured here with the reasoning so it is not re-litigated; it sits at the bottom of the main list as low-priority polish.

## 20. Marketing page content (tiered)

The 10 coming-soon marketing stubs get real content in tiers. **Tier 1a SHIPPED (2026-06-07, D-126): the Trust Center at /trust**, built on a verified data-handling inventory, replacing the Security stub (which now 308-redirects) and taking the footer's Company-column slot as "Trust". **Tier 1b SHIPPED (2026-06-07, D-127): About, Mission, Connections (renamed from Integrations with a 308 from the old route), FAQ, and Contact**, on a shared editorial shell (`components/marketing/marketing-page.tsx`) extracted from the Trust Center idiom. **The marketing transition polish SHIPPED (2026-06-07, D-128):** the public surface is unified under the (marketing) segment with a per-navigation crossfade, and the landing plays its full choreography only on cold arrival (a quick settle on in-app return; the glyph skips its opening phase). **The Trust sub-pages SHIPPED (2026-06-07, D-129):** the Trust Center is now a hub linking three deeper pages (/trust/security, /trust/control, /trust/privacy), derived from docs/SECURITY_ARCHITECTURE.md and the data-handling inventory and kept current per the D-129 standing rule. **The marketing layout/density pass SHIPPED (2026-06-07, D-130; alignment revised 2026-06-08, D-131):** the reading column carries a tightened vertical rhythm, all from the shared shell, and /trust consumes the shell (the D-127/D-129 harmonization note is resolved); the column was briefly left-anchored (D-130) then reverted to centered on a slightly wider ~736px measure (D-131), keeping the tightening and the /trust harmonization. **Tier 2 SHIPPED (2026-06-08, D-134): Blog and Documentation honest shells** on the shared shell (no posts / docs yet, said plainly; Blog points to About + Mission, Documentation offers direct help to early users). **Pricing is intentionally left as a minimal coming-soon stub** pending the business-model arc (no numbers or model explanation yet). **Tier 3 SHIPPED (2026-06-08, D-135): the Legal documents, in draft.** /legal is now a hub (mirroring Trust) linking four document sub-pages: Terms of Service, Privacy Policy, Data Processing Agreement, and Subprocessors, all on the shared shell with a prominent DRAFT-NOT-EFFECTIVE banner, bracketed placeholders preserved (entity, jurisdiction, contacts, dates, pricing/liability/indemnity), and visible reviewer-note callouts flagging every clause needing an operational capability or decision. **The marketing footer is now fully populated:** Trust (hub + 3 sub-pages), About, Mission, Connections, FAQ, Contact, Blog, Documentation are real; Pricing is an intentional coming-soon; Legal is live in draft. **Finalizing the Legal documents** (filling placeholders, counsel review, and making them effective) depends on counsel review and on building the operational commitments the draft notes flag, all under the privacy/security initiative (item 5): deletion and return, breach detection and notification, data-subject-request assistance, and subprocessor-change notification. The documents are derived from the product's truth and are to be kept current as it evolves. The privacy/security initiative (item 5) remains the home for the actual retention/deletion/compliance builds the Privacy sub-page describes as roadmap. The deeper retention/deletion/export/compliance work the Trust Center honestly defers (hard-delete purge job, account deletion and data export, GDPR/CCPA readiness, SOC 2) remains item 5 (privacy and security), not a marketing task. Standing rule from D-126: marketing claims must never drift ahead of the architecture; substantive changes to data handling or the approval model update /trust in the same commit. **Marketing restructure SHIPPED (2026-06-09, D-146):** a /features product-tour page (the workspace, agents and departments, workflows, connections, measurement, admin and governance, each section anchored and honest to shipped capability); the measurement section relocated there off the landing (which returns to its focused hero and original choreography); the standalone Connections page folded in (/connections 308-redirects to /features#connections, /integrations retargeted to skip the chain); and the footer rebalanced to Product (Features, Pricing, no workspace deep-link), Resources (Trust first, Documentation, Blog, FAQ), Company (About, Mission, Legal, Contact). **Blog retired (2026-06-12, D-159):** the honest-but-empty Blog shell left the footer and the site until there is something to say; /blog temporarily redirects to /about (a 307, not the renamed pages' 308s, so the route is not cached away against its possible return). **Named follow-on: the visual-production pass — per-feature demo videos AND documentation screenshots.** One effort, one trigger: when the product surfaces stabilize. The Features page sections are structured so an embed slots in per section when real footage exists (deliberately no placeholder video UI until then), and the documentation guides (D-158) get screenshots in the same pass; words-first was the deliberate v1 for both, to avoid screenshot and footage rot while the UI moves weekly.

---

## 21. Demo access (shared, seeded, disposable Demo Org)

Let a handful of prospects log into a seeded, RLS-isolated Demo Org and use it as super_admin, safely separate from the operator's real org. Decided model (scoping in D-049 / `docs/DEMO_ACCESS_SCOPING.md`, refined): a SHARED, SEEDED Demo Org (not per-session, not read-only), with demo users as super_admin of a throwaway org. **Step 1 SHIPPED (2026-06-08, D-132): the `organizations.is_demo` flag (migration 0064) + an idempotent seed (`supabase/seed/demo-org.sql`) that stands up one Demo Org ("Demo Workspace", slug `demo`, `is_demo = true`) and SQL-copies the real org's departments and agents into it.** The seed only reads the real org and only writes the Demo Org, with paranoid assertions; the `is_demo` flag is the cornerstone the Step-2 reset keys on. Workflow templates were deliberately deferred from the Step-1 seed (their definition jsonb embeds resolved agent ids; the clean follow-up is an `--org-id` flag on the slug-resolving `scripts/seed-workflow-templates.ts`, not fiddly untested jsonb SQL; **that `--org-id` flag SHIPPED 2026-06-22, D-165**, so the Demo Org can carry the starter template gallery). **Step 2 SHIPPED (2026-06-08, D-133): the access link + reset tooling.** A `/demo/<token>` route (migration 0065 adds the `demo_invitations` token table, storing only a SHA-256 hash) signs a prospect into the Demo Org as super_admin with NO email: it creates a synthetic `…@legalos-internal.invalid` user and consumes a server-generated magic link server-side. Tokens are single-use (an atomic pending→consumed claim), rate-limited, and minted by `npm run mint-demo-token`. A `npm run reset-demo-org` script offers a default soft reset (clear accumulated activity; restore seeded structure, org-scoped settings, and the demo users' super_admin baseline; keep demo users/links) and a `--hard` nuke-and-reseed, behind a layered guard (explicit `--org-id`, must be `is_demo = true`, must not equal the real org id, re-asserted before the destructive block, slug-typed confirmation, every statement org-scoped); the shared structure-restore lives in the guarded `seed_demo_org_structure()` SQL function (0065). **Connection org-scoping security fix SHIPPED (2026-06-08, D-136):** the must-fix that gated showing the demo to untrusted prospects. `connection_policy` and `connections` (including bring-your-own model keys) were built single-tenant with no `organization_id`; the live Demo Org made the gap real (a super_admin, including a demo user, could rewrite the global connection policy and, more seriously, a saved BYO model key resolved globally so one tenant's credential could route every org's inference). Both tables are now org-scoped (migration 0066: `organization_id`, org-fenced RLS, per-org BYO unique index, backfilled to the real org whose behavior is unchanged), the model-credential resolver and the service-role connection/MCP reads filter by org, and the project's first cross-org isolation tests were added (resolver-level; a live-Postgres RLS harness remains the additive next step). The reset script can org-scope `connection_policy` as a follow-up now that the column exists. **Demo-token admin UI + time-window links SHIPPED (2026-06-22, D-166):** a platform-owner surface (`/workspace/platform/demo-access`) to mint labeled, time-window demo links (default 14 days, configurable 7/14/30, same demo user across visits), list who has access with status/dates/last-opened, and revoke; the link model shifted from single-use to time-window (migration 0074 adds `label` + `last_accessed_at` and allows status `active`), and a collapsible status panel documents demo behavior, held current by the done-definition rule. **Additive future:** a TTL cleanup cron / auto-delete of stale demo users; and the full per-session-isolated design (fresh org per user) from `docs/DEMO_ACCESS_SCOPING.md`. (Seeding the workflow templates into the demo org, the `--org-id` follow-up, SHIPPED 2026-06-22, D-165.)

---

## Backlog (unprioritized)

Items recorded for future consideration that haven't been actively prioritized into the main list above. Some were previously tracked in CHATBOT_HANDOFF.md's deferred-work section; some are product directions not yet evaluated. Promote to the main list when an item is worth real work.

### Sync pipeline Shape B

Architectural direction for the C4L sync pipeline. Shape B was a previously-evaluated approach for how C4L plugins get periodically re-synced from vendor sources (specifics in C4L design history). Backlogged because the current import pipeline (run scripts/import-c4l-plugin.ts manually) works for the operator's current scale; productizing the sync flow is real engineering not yet justified by demand.

**Update 2026-06-11: partially served by D-151.** The platform refresh now detects BOTH content drift (D-112) and connector drift (D-151) against upstream in one button press, so the detection half of any future Shape B is built. What remains is the apply/productize side, and for connectors that side deliberately stays a human, vetted code change (the catalog is the compiled-in trust ceiling).

### Regulatory monitors

Some form of regulatory change tracking: alerting users when laws or regulations relevant to their practice areas change. A product direction surfaced during earlier exploration; not yet evaluated for scope or fit.

### Public/private repo decision

Strategic decision about whether the legalOS codebase stays private (current state) or becomes public/open-source. Has implications for licensing, contribution model, IP strategy, marketing positioning. Backlogged pending operator readiness to engage with the decision.

### Rail auto-expand toggle preference

The current force-expand logic (from polish #1, extended in Stage 4 of the workspace arc) automatically expands a rail group when its leaf or group landing is the active route, unless the user has explicitly toggled the group's collapse state this session. The operator expressed a preference for "full control by user" (no auto-expand) but chose to live with the current behavior. If the preference firms up, the fix is to remove the forceExpanded computation from CollapsibleRailGroup and rely solely on user toggle + persisted preference.

### Rail caption hover-bar width tune

After Stage 4 of the workspace arc moved the chevron to the right of the caption, the caption's `flex-1` means its hover bg-fill now spans the full row width up to the chevron. It may read as too wide depending on visual evaluation; if so, narrowing the caption's bg-fill is a small tune. The operator hasn't flagged it as an issue; documented in case future evaluation surfaces it.

### Admin config surface

Substructure of the admin section revamp (item 3). Specific admin UI for configuring application-wide settings. Likely absorbed when item 3 is scoped.

### Matter-workspace management

Substructure that overlaps with the Tracker-UI surface (item 13) and the admin section revamp (item 3). Specific UI for managing the relationship between legal matters and workspace state. Likely clarifies as items 13 and 3 are scoped.

### Router-skills Workflows surface

Substructure of the Workflows group's eventual product surface. The router-skills pattern (Pattern #1 in docs/C4L_DEFERRED_SKILLS.md) is the C4L approach for multi-skill agents routing between sub-skills. A Workflows UI surface would expose this pattern. Likely clarifies when item 12 (Template Library concept) is defined.
